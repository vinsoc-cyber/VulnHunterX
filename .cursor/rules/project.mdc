---
description: Project context for CodeQLxLLM - CodeQL + LLM bug verification (Vulnhalla-style)
alwaysApply: true
---

# Project context (CodeQLxLLM)

This repo is a **Python framework** that combines **CodeQL** static analysis with **LLM verification** to reduce false positives. Main language: **Python 3.12+**. Stack: **litellm** (OpenAI/Ollama), **python-dotenv**, **PyYAML**.

## Pipeline

clone -> CodeQL database -> analyze (SARIF) -> extract-context -> verify (LLM). Optional fuzz stages 5-8 for C/C++ (build-sanitized, extract-fuzz-context, generate-fuzz-drivers, fuzz-run).

## Structure

- **CLI:** `codeql-llm` in [src/codeql_llm/cli/main.py](src/codeql_llm/cli/main.py). Commands: clone, analyze, extract-context, verify, build-sanitized, extract-fuzz-context, generate-fuzz-drivers, fuzz-run, check-env.
- **Packages:** codeql (DB, analysis), sarif (parse SARIF -> Finding), verification (engine + LLM), llm (client, prompts), questions (guided questions), context (extractor, provider), fuzz (sanitized build, target selection, drivers, runner).
- **Config:** [config/confirm_findings.yaml](config/confirm_findings.yaml), [config/repos.yaml](config/repos.yaml), [config/prompts/guided_questions.yaml](config/prompts/guided_questions.yaml).
- **Paths:** Repos `repos/<lang>/<name>/`, DBs `databases/<lang>/<name>/`, SARIF `output/sarif/<lang>/<repo>.sarif`, results `output/results/`, context `output/context/<repo>/`.

## Conventions

- Use **pathlib.Path** and type hints. Subprocess: **list argv only**, no shell. Docstrings: Google-style (Args, Returns) for public APIs. Follow [pyproject.toml](pyproject.toml) Ruff config.

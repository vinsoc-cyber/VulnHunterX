# Phase 2: cloned repos; all stage outputs under output/
repos/
output/
.codeql_build.sh
# Env and build
.env
.mypy_cache/
.venv/
*.egg-info/
build/
dist/
.pytest_cache/
__pycache__/
.vscode/
# Output
output/
*.sarif
uv.lock
.coverage
.env*
test-apps

# Benchmarks — large datasets and results are git-ignored
benchmarks/datasets/
benchmarks/results/
# The real model matrix is local; only the .example template is tracked.
benchmarks/config/models.yaml
# Track the sanitized credential templates (past the .env* rule above).
!benchmarks/config/.env.*.example

# IDE and local tooling — not committed
.claude/
.cursor/
CLAUDE.md
.ruff_cache/
docs/paper/
docs/superpowers/
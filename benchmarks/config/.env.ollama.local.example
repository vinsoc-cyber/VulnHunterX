# VulnHunterX — local Ollama server (model-matrix template)
# Copy to a repo-root `.env.ollama.local` and adjust for your host.
LLM_PROVIDER=ollama

LLM_MODEL=qwen3.5:cloud

# Point at your local/LAN Ollama server.
OLLAMA_API_BASE=http://localhost:11434

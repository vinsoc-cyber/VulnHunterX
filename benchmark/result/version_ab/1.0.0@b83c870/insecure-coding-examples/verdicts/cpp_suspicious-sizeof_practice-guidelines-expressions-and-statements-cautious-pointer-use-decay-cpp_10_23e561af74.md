# cpp/suspicious-sizeof @ practice/guidelines/expressions_and_statements/cautious_pointer_use_decay.cpp:10

**Verdict:** FP · **Confidence:** High (0.9) · **Truth:** not-real · **Grade:** CORRECT · **Iterations:** 2

## Reasoning

The CodeQL pattern is real: line 10 applies `sizeof` to pointer parameter `ages` from line 8. However, the exact flagged result is only printed on line 10 and is not used in any allocation, copy, bounds check, indexing, or other memory-sensitive operation, so the provided code does not show an exploitable security vulnerability. [second-opinion pass after 1-iter high-conf FP]

## Answers

1. Step 1 / source: The flagged line is line 10: `std::cout << sizeof(ages) << '\n';`. The construct CodeQL describes is present: `ages` is declared as `const int * ages` on line 8, so `sizeof(ages)` on line 10 evaluates to the size of the pointer type. No attacker-controlled source is visible; the pre-fetched caller creates a fixed local array `int ages[] = { 15, 30, 60 };` and passes it to `decay(ages)`.
2. Step 2 / data trace: In the caller, local array `ages` is initialized with constants, then passed to `decay(ages)`, where it decays to `const int * ages` on line 8. Inside `decay`, `ages` is not dereferenced, indexed, copied from, or used as a length. It is used only as the operand to `sizeof` on line 10, and that numeric result is streamed to `std::cout` on line 10.
3. Step 3 / validation or sanitization: There is no validation or sanitization on lines 8-10. For this finding, the relevant observed safety mechanism is not input sanitization but output-only use: line 10 sends the computed pointer-size value only to `std::cout`; it is not used for allocation, memcpy/memmove, indexing, loop bounds, or buffer sizing.
4. Step 4 / sink: The sink is line 10, `std::cout << sizeof(ages) << '\n';`. The operation that CodeQL considers suspicious is applying `sizeof` to a pointer parameter rather than to an array. In the visible code, the result is only printed, which makes the specific flagged use non-security-dangerous.
5. Step 5 / framework or library protections: No framework protection is involved. The relevant C++ library operation is `std::cout << ...` on line 10, which outputs the computed `sizeof` value. This is not an automatic protection, but it is the complete visible use of the value.
6. Step 6 / privilege or authentication: Not visible in provided context. The only shown entry point is a simple `main` that calls `decay(ages)` with a constant local array; no authentication or external triggering mechanism is shown.
7. Step 7 / concrete impact: No concrete security impact is visible. Even though line 10 does compute the pointer size rather than the array length, the value is merely printed and does not control memory allocation, memory copy length, bounds checking, or pointer arithmetic.
8. Step 8 / weakest link and verification requested: (a) Specific observed defense/benign mechanism: line 10 uses the suspicious value only as an argument to `std::cout <<`, i.e. output-only; there is no memory-safety-sensitive use in lines 8-13. (b) This covers all reachable paths within the provided function because `decay` contains only the parameter declaration on line 8, comments on lines 9, 11-12, and the single executable statement on line 10; every call to `decay` reaches the same output-only sink. (c) The SAST tool flagged this because `cpp/suspicious-sizeof` looks for `sizeof` applied to a pointer where an array size may have been intended; that pattern is genuinely present because `ages` is `const int *` on line 8 and `sizeof(ages)` appears on line 10. The observed benign mechanism does not prove the expression is semantically intended; it proves only that this particular flagged use is not exploitable as a security vulnerability.

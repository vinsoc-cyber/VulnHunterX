# cpp/signed-overflow-check @ practice/if_constexpr.cpp:14

**Verdict:** FP · **Confidence:** Medium (0.78) · **Truth:** real · **Grade:** MISS · **Iterations:** 2

## Reasoning

The CodeQL pattern is genuinely present and the fixed expression `INT_MAX + 256` exceeds the `int` maximum, but the operands are compile-time constants rather than attacker-controlled data. In the shown `if constexpr` context, this is a correctness/ill-formed-constant-expression issue, not a demonstrated exploitable runtime overflow flowing to a dangerous sink. [second-opinion pass: TP challenge on correctness rule]

## Answers

1. Step 0: The flagged line is present at line 14: `if constexpr ((first_len + second_len) <= buf_len) {`. The rule-described construct is present there: a signed `int` addition, `first_len + second_len`, is used in an overflow-style bounds check.
2. (a) Operand and intermediate types: `first_len` is declared `const int` on line 8, `second_len` is declared `const int` on line 9, and `buf_len` is declared `const int` on line 10. In `first_len + second_len` on line 14, both operands are `int`, so the usual arithmetic conversions produce an intermediate result of type `int`. The maximum value of that intermediate type is `std::numeric_limits<int>::max()`, which is exactly the value assigned to `first_len` on line 8.
3. (b) Operand values: the values come from compile-time/local constants, not attacker-controlled input. `first_len` is fixed to `std::numeric_limits<int>::max()` on line 8; `second_len` is fixed to literal `256` on line 9; `buf_len` is fixed to literal `256` on line 10. No file, network, argv, database, or user-controlled source is visible.
4. (c) Given the visible bounds, the signed addition can exceed the intermediate type's maximum. This is addition rather than multiplication: `first_len + second_len` equals `INT_MAX + 256`, and the intermediate type is `int`, whose maximum is `INT_MAX`. Therefore `INT_MAX + 256 > INT_MAX`, so the arithmetic expression on line 14 overflows the `int` intermediate if evaluated as ordinary signed arithmetic.
5. (d) There is no widened result visible. The overflowing expression is used directly in the `if constexpr` condition on line 14. The branch contains dangerous-looking `memcpy` operations using `first_len` and `second_len` as sizes/offsets on lines 15-16, but because line 14 is `if constexpr`, the condition must be a valid compile-time constant expression; signed overflow in such an expression makes the program ill-formed rather than creating an attacker-reachable runtime bounds-check bypass in the shown code.
6. (e) The file path is `practice/if_constexpr.cpp`, and the values are fixed fixtures/constants on lines 8-10. The provided snippet does not explicitly label it as a test, benchmark, or fuzz harness, but the operands are visibly fixed constants rather than attacker input.
7. Original guided Step 1 source answer: No potentially dangerous external source is visible. All relevant values originate from local constants in `main`: lines 8-10.
8. Original guided Step 2 trace answer: `std::numeric_limits<int>::max()` → `first_len` on line 8; `256` → `second_len` on line 9; `256` → `buf_len` on line 10; `first_len + second_len` is evaluated in the `if constexpr` condition on line 14; the guarded block would use these constants in `memcpy` calls on lines 15-16.
9. Original guided Step 3 validation answer: No validation, checked arithmetic, widening cast, or safe rearranged comparison is visible before the addition on line 14.
10. Original guided Step 4 sink answer: The flagged sink is the signed `int` addition in the condition on line 14. The branch also contains `memcpy` calls on lines 15-16, but no attacker-controlled flow to those calls is visible.
11. Original guided Step 5 framework/library protections answer: No framework protection is involved. The relevant C++ language feature is `if constexpr` on line 14, which requires a valid compile-time constant expression; that prevents this from being an attacker-triggerable runtime overflow path in the shown code.
12. Original guided Step 6 privilege answer: No attacker privilege or authentication state is applicable from the provided context because no attacker-controlled input reaches the operands.
13. Original guided Step 7 impact answer: The concrete issue shown is a correctness/type-hygiene problem: fixed constants produce an overflowing signed `int` expression on line 14. A concrete security impact such as RCE, data theft, privilege escalation, or DoS from attacker-controlled data is not demonstrated in the provided code.
14. Original guided Step 8 weakest-link answer: The weakest coding pattern is the overflow-prone check on line 14, which computes `first_len + second_len` before proving the sum is representable. However, because the operands are fixed constants and the expression is in `if constexpr`, the shown path does not establish an exploitable runtime vulnerability.

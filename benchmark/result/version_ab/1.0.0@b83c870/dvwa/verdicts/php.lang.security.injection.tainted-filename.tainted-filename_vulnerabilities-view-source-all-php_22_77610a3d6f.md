# php.lang.security.injection.tainted-filename.tainted-filename @ vulnerabilities/view_source_all.php:22

**Verdict:** TP · **Confidence:** Medium (0.78) · **Truth:** real · **Grade:** CORRECT · **Iterations:** 3

## Reasoning

The flagged sink is present and receives a filename derived directly from `$_GET['id']` with no visible validation before `file_get_contents`. Although the Semgrep message frames this as SSRF, the exploitable issue shown by the code is tainted local filename/path traversal leading to unauthorized source-file disclosure.

## Data flow

HTTP query parameter `$_GET['id']` checked only for existence at vulnerabilities/view_source_all.php:11 → assigned directly to `$id` at vulnerabilities/view_source_all.php:12 → interpolated into `"./{$id}/source/high.php"` at vulnerabilities/view_source_all.php:22 → passed to `file_get_contents` at vulnerabilities/view_source_all.php:22 → resulting contents processed after the read by `str_replace` and `highlight_string` at vulnerabilities/view_source_all.php:23-24. Additional requested context for `dvwaPageStartup`, `$_GET`, and `DVWA_WEB_PAGE_TO_ROOT` was unavailable and provides no visible defense.

## Answers

1. Step 0 / Flagged line location: The flagged line is present at line 22 and its exact text is `$highsrc = @file_get_contents("./{$id}/source/high.php");`. It is in the top-level script context of `vulnerabilities/view_source_all.php`; no named enclosing function is visible. The construct described by the rule is present: a filename passed to `file_get_contents` is built using `$id`, which comes from request input.
2. Step 1 / Source: The dangerous data originates from user-controlled HTTP query input. Line 11 checks for the presence of `$_GET['id']`, and line 12 assigns `$_GET['id']` directly to `$id`. The newly provided context for `global:$_GET` is unavailable, so it does not add any visible sanitizer or type guarantee.
3. Step 2 / Trace: In `vulnerabilities/view_source_all.php`, line 11 performs a presence check with `array_key_exists("id", $_GET)`. Line 12 assigns `$_GET['id']` to `$id`. Line 22 interpolates `$id` into the path string `"./{$id}/source/high.php"` and passes that path to `file_get_contents`. Lines 23-24 then process `$highsrc` after the file has already been read.
4. Step 3 / Validation/Sanitization: No sufficient validation or sanitization is visible before the flagged sink. Line 11 only checks that the parameter exists; it does not restrict `$id` to known module names, remove path traversal sequences, canonicalize the path, or enforce that the resolved path stays inside an intended directory. The later `switch ($id)` starts at line 30, after the file reads on lines 14, 18, 22, and 26, so it cannot protect the flagged sink. The additional context for `function:dvwaPageStartup`, `global:$_GET`, and `global:DVWA_WEB_PAGE_TO_ROOT` is unavailable and therefore does not change this answer.
5. Step 4 / Sink: The sink is `file_get_contents` on line 22. The unsafe operation is server-side file access using a filename partly controlled by the HTTP request. The rule message names SSRF, but from the visible code the stronger classification is tainted filesystem path / path traversal / unauthorized local file read, because the path is prefixed with `./` and suffixed with `/source/high.php`.
6. Step 5 / Framework/Library Protections: No relevant framework or library protection is visible. `dvwaPageStartup(array('authenticated'))` on line 6 appears to enforce authentication, but its body was not provided and no visible evidence shows it validates `$_GET['id']`. PHP `file_get_contents` itself does not automatically whitelist paths or prevent traversal in the supplied filename.
7. Step 6 / Required Privilege: Based on line 6, the attacker must be authenticated. No admin-only check is visible. If an authenticated user can request this script with an `id` query parameter, they can reach the line 22 sink.
8. Step 7 / Security Impact: The concrete impact is unauthorized server-side file read/source disclosure of files reachable through the constructed path pattern `./<attacker-controlled-id>/source/high.php`. Because the contents are assigned to `$highsrc` on line 22 and then syntax-highlighted on line 24, the read content is apparently intended for display. Direct SSRF is not clearly demonstrated from the visible path shape, but local file disclosure via tainted path is a real security impact at the same sink.
9. Step 8 / Weakest Link: The weakest link is that `$id` is used in the file path at line 22 before any whitelist or canonical path enforcement. The only check before the sink is existence of the parameter on line 11. The later `switch ($id)` beginning at line 30 is too late to defend the file read.

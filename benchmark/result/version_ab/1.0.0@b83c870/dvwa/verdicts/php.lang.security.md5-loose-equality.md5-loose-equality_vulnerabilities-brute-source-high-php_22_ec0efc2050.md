# php.lang.security.md5-loose-equality.md5-loose-equality @ vulnerabilities/brute/source/high.php:22

**Verdict:** FP · **Confidence:** High (0.91) · **Truth:** not-real · **Grade:** CORRECT · **Iterations:** 2

## Reasoning

Step 0 confirmed the flagged line is present: `if( $result && mysqli_num_rows( $result ) == 1 ) {` on line 22, and it does contain `==`. However, the specific md5 loose-equality construct is absent at that line: the comparison is between a mysqli row count and integer `1`, while the md5 value from line 16 is only used in the SQL query on line 19. [second-opinion pass after 1-iter high-conf FP]

## Answers

1. Q1: The potentially dangerous data originates from HTTP GET parameters: `$_GET['username']` on line 8 and `$_GET['password']` on line 13. For the flagged sink on line 22, however, the compared value originates from `mysqli_num_rows($result)`, where `$result` is created by `mysqli_query(...)` on line 20.
2. Q2: Password flow: `$_GET['password']` line 13 → `stripslashes($pass)` line 14 → `mysqli_real_escape_string(..., $pass)` line 15 → `md5($pass)` line 16 → interpolated into SQL string line 19 → executed by `mysqli_query(...)` line 20 → resulting row count checked by `mysqli_num_rows($result) == 1` line 22. Username flow: `$_GET['username']` line 8 → `stripslashes($user)` line 9 → `mysqli_real_escape_string(..., $user)` line 10 → interpolated into SQL line 19.
3. Q3: Sanitization visible: `stripslashes` on lines 9 and 14, and `mysqli_real_escape_string` on lines 10 and 15. For the specific md5 loose-equality issue, the decisive observation is not sanitization but operand identity: the md5 value created on line 16 is not an operand of the loose comparison on line 22.
4. Q4: The flagged sink is line 22: `if( $result && mysqli_num_rows( $result ) == 1 ) {`. The operation is loose equality `==`, but it compares the row count returned by `mysqli_num_rows($result)` to integer literal `1`; it does not compare an md5 hash or md5-like attacker-controlled string.
5. Q5: No framework auto-protection is relevant to the flagged md5 loose-equality finding. The visible CSRF call `checkToken(...)` on line 5 is unrelated to md5 type juggling. The relevant concrete mechanism is the mysqli API call on line 22: `mysqli_num_rows($result)` returns a row count, not the md5 string from line 16.
6. Q6: The code path is reachable when `isset($_GET['Login'])` is true on line 3 and `checkToken(...)` on line 5 permits execution. Based only on the snippet, this appears to be a login path likely reachable by an unauthenticated user with a valid session token; exact token acquisition behavior is not visible.
7. Q7: If an md5 hash were compared using PHP loose equality, impact could be authentication bypass through PHP type juggling, especially with magic hashes like `0e...`. In this code, that impact is not demonstrated at the flagged line because line 22 compares a database row count to `1`, not an md5 hash.
8. Q8: Verification requested: (a) Specific defense/mechanism observed: line 22’s operands are fixed in code as `mysqli_num_rows($result)` and integer literal `1`; the md5 value from line 16 is not present in the comparison. This is a concrete operand/type/source separation, not an assumed external defense. (b) It covers all reachable paths to the flagged sink because every execution of line 22 uses the same expression shown on line 22; there is no alternate branch in the provided code where `$pass` or `md5($pass)` is compared with `==`. (c) The SAST rule appears to look for loose comparisons involving md5 values, but the flagged line only contains a loose row-count comparison. The observed mechanism directly refutes the rule condition at the flagged sink: no md5 value is compared with `==` on line 22.

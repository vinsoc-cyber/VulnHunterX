# javascript.browser.security.eval-detected.eval-detected @ vulnerabilities/javascript/source/high.js:1

**Verdict:** FP · **Confidence:** High (0.88) · **Truth:** not-real · **Grade:** CORRECT · **Iterations:** 3

## Reasoning

The flagged `eval` sink is real and present on line 1, but the evaluated string is visibly derived from hardcoded literals in `a` and deterministic local unpacking transformations on the same line. The additional context did not reveal any external source or cross-function path into the eval argument, so there is no clearly exploitable CWE-95 code-injection path for this finding.

## Data flow

vulnerabilities/javascript/source/high.js:1 hardcoded array literal `var a=[...]` → vulnerabilities/javascript/source/high.js:1 array rotation IIFE mutates `a` using `push(shift())` → vulnerabilities/javascript/source/high.js:1 lookup helper `var b=function(c,d){... return a[c]; }` → vulnerabilities/javascript/source/high.js:1 packed code `b('0x7')` and replacement table `b('0x8').split('|')` passed into unpacking function → vulnerabilities/javascript/source/high.js:1 repeated `replace(new RegExp(...), g[f])` transformations → vulnerabilities/javascript/source/high.js:1 returned string passed to `eval(...)`; additional requested contexts `global:a`, `global:b`, and `all_callers:<unknown>` were unavailable and add no new source or transformation.

## Answers

1. Step 0 / Flagged line location: The flagged line is present at vulnerabilities/javascript/source/high.js:1 in function `<unknown>` / top-level script scope. The line is a single minified statement beginning `var a=['fromCharCode','toString','replace','BeJ','\x5cw+',...` and it contains the exact dangerous sink fragment `eval(function(d,e,f,g,h,i){h=function(j){return(j<e?'':h(parseInt(j/e)))+((j=j%e)>0x23?String[b('0x0')](j+0x1d):j[b('0x1')](0x24));};...}(b('0x7'),0x3e,0x137,b('0x8')[b('0x9')]('|'),0x0,{}));`. The construct described by the rule, `eval()`, is present on the flagged line.
2. Step 1 / Source: The potentially evaluated data originates from literals assigned in `var a=[...]` on line 1. The new context for `global:a`, `global:b`, and `all_callers:<unknown>` is unavailable and does not show any external source. In the visible code, there is no user input, request data, file data, network data, database data, cookie, URL parameter, or DOM-derived value flowing into the argument of `eval`.
3. Step 2 / Data trace: On line 1, local/global script variable `a` is assigned a hardcoded array literal. On line 1, `(function(c,d){var e=function(f){while(--f){c['push'](c['shift']());}};e(++d);}(a,0x1f4));` mutates that same literal array by rotating entries. On line 1, `var b=function(c,d){c=c-0x0;var e=a[c];return e;};` reads entries from `a`. On line 1, `b('0x7')` supplies the packed code string to parameter `d`, and `b('0x8')[b('0x9')]('|')` supplies the replacement table to parameter `g`. On line 1, the unpacking function transforms `d` using repeated `replace` calls with generated regular expressions. On line 1, the final returned string is passed directly to `eval(...)`.
4. Step 3 / Validation, sanitization, or encoding: No validation, sanitization, or security encoding is applied before `eval` on line 1. The visible operations are obfuscation/unpacking operations: array rotation, indexed lookup through `b`, `split('|')`, `RegExp`, and `replace`. These would not be sufficient sanitization if the evaluated string were attacker-controlled. However, the visible evaluated string is derived from hardcoded literals assigned on the same line.
5. Step 4 / Sink: The sink is the `eval(...)` call on line 1 in top-level/function `<unknown>` scope. The dangerous operation is dynamic execution of the string returned by the unpacking function.
6. Step 5 / Framework or library protections: No framework or library protection is visible. There is no shown CSP, sandbox, template auto-escaping, ORM parameterization, or other automatic protection relevant to `eval`. The reason this is not confirmed exploitable is not a framework defense; it is that the visible data reaching `eval` is hardcoded rather than externally controlled.
7. Step 6 / Required privilege or authentication: Not visible in provided context. The snippet does not show how the JavaScript file is loaded or which users can trigger it. The code appears to execute when the script is loaded, but authentication or privilege requirements are not shown.
8. Step 7 / Security impact if attacker controls this data: If an attacker could control the string passed to `eval` on line 1, the impact would be arbitrary JavaScript execution in the browser context, including possible token/session theft, DOM manipulation, phishing, or actions as the victim user. In the provided code and additional context, attacker control of the evaluated string is not shown.
9. Step 8 / Weakest link: The weakest link is the direct use of `eval` on line 1 with no sanitizer. The defense against this specific CWE-95 finding is that the evaluated input is visibly constructed from hardcoded literals assigned immediately in the same top-level line, then transformed locally before evaluation; no external source is visible in the original snippet or the additional unavailable context.

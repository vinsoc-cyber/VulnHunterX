# php.lang.security.injection.tainted-filename.tainted-filename @ vulnerabilities/view_source_all.php:26

**Verdict:** FP · **Confidence:** Medium (0.74) · **Truth:** real · **Grade:** MISS · **Iterations:** 3

## Reasoning

The flagged line does contain tainted user input in a `file_get_contents()` filename, but the reported CWE-918 SSRF path is not clearly exploitable because the constructed target is forced to start with `./`, preventing a user-controlled leading URL scheme. No additional context changed the trace; the remaining concern is an unvalidated local path component, but a concrete exploitable file disclosure at this flagged sink is not established in the provided code.

## Data flow

vulnerabilities/view_source_all.php:11 `array_key_exists("id", $_GET)` → vulnerabilities/view_source_all.php:12 `$id = $_GET['id'];` → vulnerabilities/view_source_all.php:26 interpolate `$id` into `"./{$id}/source/impossible.php"` → vulnerabilities/view_source_all.php:26 `file_get_contents()` filename sink. Requested additional context for `dvwaPageStartup`, `dvwaPageNewGrab`, and PHP globals was unavailable and does not change the visible flow.

## Answers

1. Step 0 / flagged line location: The flagged line is present at vulnerabilities/view_source_all.php:26 and is exactly: `$impsrc = @file_get_contents("./{$id}/source/impossible.php");`. The Semgrep-described construct is present on that line: a filename passed to `file_get_contents()` contains user-influenced `$id`. The provided function is listed as `<unknown>`; based on the snippet, this appears to be top-level PHP script code rather than a named function.
2. Step 1: The potentially dangerous data originates from HTTP query-string user input. The code checks `array_key_exists("id", $_GET)` at vulnerabilities/view_source_all.php:11, then assigns `$_GET['id']` to `$id` at vulnerabilities/view_source_all.php:12.
3. Step 2: The data flow is: `$_GET['id']` at vulnerabilities/view_source_all.php:11-12 → direct assignment to `$id` at vulnerabilities/view_source_all.php:12 → interpolation into the string `"./{$id}/source/impossible.php"` at vulnerabilities/view_source_all.php:26 → use as the filename argument to `file_get_contents()` at vulnerabilities/view_source_all.php:26. Additional requested context for `dvwaPageStartup`, `dvwaPageNewGrab`, and PHP globals was unavailable, so it does not add any new transformation or sanitizer to the trace.
4. Step 3: No validation, sanitization, allowlist, canonicalization, or encoding of `$id` is visible before the flagged sink at vulnerabilities/view_source_all.php:26. The `switch ($id)` at vulnerabilities/view_source_all.php:30-75 occurs after all four `file_get_contents()` calls, including the flagged line, so it cannot protect the sink. However, for the specific CWE-918 SSRF concern, the sink path is visibly prefixed with `./` at line 26, which prevents `$id` from becoming the leading URI scheme such as `http://` or `https://`.
5. Step 4: The sink is `file_get_contents()` at vulnerabilities/view_source_all.php:26. The dangerous operation flagged by the rule is using tainted data as a filename/stream target. For SSRF specifically, PHP stream-wrapper URL access would require a URL-like target, but the visible target starts with the fixed local relative path prefix `./`.
6. Step 5: No automatic framework or library filename protection is visible. `dvwaPageStartup(array('authenticated'))` at vulnerabilities/view_source_all.php:6 appears to enforce authentication, but its body was unavailable and it is not visibly a filename sanitizer. PHP `file_get_contents()` does not sanitize paths. The only visible protection relevant to SSRF is the fixed `./` prefix in the filename string at line 26.
7. Step 6: The visible code requires an authenticated user because vulnerabilities/view_source_all.php:6 calls `dvwaPageStartup(array('authenticated'))`. The provided context does not show whether a stricter role, such as admin, is required.
8. Step 7: If `$id` were used as a full URL, the impact could be SSRF. In this snippet, SSRF is not clearly exploitable because the filename at line 26 always begins with `./`, so attacker input cannot directly form a leading remote URL wrapper. A separate local-path issue is theoretically possible because `$id` is not allowlisted, but the visible path also has a fixed `/source/impossible.php` suffix and the snippet does not show later output or a concrete sensitive-file target.
9. Step 8: The weakest link is the absence of pre-sink validation or allowlisting for `$id` before it is used in filesystem paths at vulnerabilities/view_source_all.php:14, 18, 22, and 26. For the specific flagged SSRF finding, though, the fixed `./` path prefix at line 26 is the visible defense that prevents user input from controlling the URI scheme.

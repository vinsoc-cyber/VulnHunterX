# yaml.github-actions.security.run-shell-injection.run-shell-injection @ .github/workflows/docker-image.yml:29

**Verdict:** TP · **Confidence:** Medium (0.78) · **Truth:** not-real · **Grade:** FALSE-ALARM · **Iterations:** 4

## Reasoning

The flagged line is a real GitHub Actions shell `run:` block, and untrusted `github` context data, particularly `${{ github.ref }}`, is interpolated directly into shell syntax on lines 33-34 with no visible shell-safe sanitization or `env:` indirection. Although the exact repository permission model is unavailable, the vulnerable pattern is present on a reachable workflow path and can lead to runner command execution if a user can trigger the workflow with a controlled ref.

## Data flow

GitHub workflow trigger (`workflow_dispatch` at line 4; `push` at lines 5-7) → GitHub context value `${{ github.ref }}` (lines 33-34) → direct interpolation into the shell `run:` block starting at line 29 → shell parses and executes assignments/conditional at lines 33-34 → derived `VERSION` is used in Docker tag at line 42. Additional direct interpolations: `${{ github.repository_owner }}` line 31 → `IMAGE_ID` line 32 → Docker tags lines 42-43; `${{ github.sha }}` line 36 → `COMMIT` → Docker tag line 43.

## Answers

1. Step 0 / flagged line: The flagged line is `.github/workflows/docker-image.yml:29`, exact text: `run: |`. It is in function `<unknown>` because this is a GitHub Actions YAML workflow, not PHP code. The rule’s construct is present: line 29 starts a shell `run:` block, and direct `${{ github.* }}` interpolation appears inside that block on lines 31, 33, 34, and 36.
2. Step 1: The potentially dangerous data originates from GitHub Actions `github` context values. Specifically, `${{ github.repository_owner }}` is used on line 31, `${{ github.ref }}` is used on lines 33 and 34, and `${{ github.sha }}` is used on line 36. For shell injection, the most relevant source is `${{ github.ref }}` because refs can contain attacker-influenced branch or tag names in workflows that can be triggered on attacker-controlled refs.
3. Step 2: Data flow: workflow execution is enabled by `workflow_dispatch` on line 4 and `push` on lines 5-7. The shell block begins at line 29. `${{ github.repository_owner }}` flows into `IMAGE_ID=ghcr.io/${{ github.repository_owner }}/dvwa` on line 31, then through `tr` on line 32, then into Docker tags on lines 42-43. `${{ github.ref }}` flows directly into `VERSION=$(echo "${{ github.ref }}" | sed -e 's,.*/\(.*\),\1,')` on line 33 and into `[[ "${{ github.ref }}" == "refs/tags/"* ]]` on line 34; the derived `VERSION` is later used in a Docker tag on line 42. `${{ github.sha }}` flows into `COMMIT=$(echo "${{ github.sha }}" | cut -c 1-7)` on line 36, then into a Docker tag on line 43.
4. Step 3: No adequate validation, sanitization, or shell-safe encoding is visible. The `tr`, `sed`, and `cut` operations on lines 32, 33, and 36 are transformations performed by the shell after GitHub expression interpolation has already occurred. Double-quotes around `${{ github.ref }}` on lines 33 and 34 are not sufficient to prevent shell command substitution or other shell interpretation after interpolation. No `env:` indirection with quoted shell variables is used.
5. Step 4: The sink is the GitHub Actions shell execution block beginning at line 29. The unsafe operation is direct pre-shell interpolation of `${{ github.* }}` values into shell commands, especially `${{ github.ref }}` on lines 33 and 34.
6. Step 5: No framework or library protection is visible. GitHub Actions does not automatically shell-escape direct `${{ github.* }}` interpolation inside a `run:` block. The additional requested contexts for workflow permissions, branch protection, tag protection, collaborator permissions, and GitHub context were unavailable, and they do not provide any visible defense in the supplied code.
7. Step 6: Based on the visible workflow, an attacker would need the ability to trigger this workflow with a controlled ref, such as via `workflow_dispatch` on line 4 or a permitted push path under lines 5-7. The `push` trigger is restricted to `master` on lines 6-7, which limits that path, but the manual-dispatch path remains present in the visible workflow. Exact repository permission requirements are not visible.
8. Step 7: If an attacker controls the interpolated ref, the concrete impact is shell command execution in the GitHub Actions runner. That can lead to theft or misuse of CI credentials such as the token used by the registry login step on lines 21-26 and tampering with the Docker image build/push performed on lines 41-44.
9. Step 8: The weakest link is direct `${{ github.ref }}` interpolation into the shell script on lines 33 and 34 without a protective `env:` variable boundary or shell-safe escaping. No complete defense is visible in the provided code.

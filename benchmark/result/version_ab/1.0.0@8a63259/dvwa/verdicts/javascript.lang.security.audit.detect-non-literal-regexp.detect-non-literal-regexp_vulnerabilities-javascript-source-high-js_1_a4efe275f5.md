# javascript.lang.security.audit.detect-non-literal-regexp.detect-non-literal-regexp @ vulnerabilities/javascript/source/high.js:1

**Verdict:** FP · **Confidence:** High (0.9) · **Truth:** not-real · **Grade:** CORRECT · **Iterations:** 3

## Reasoning

The flagged non-literal RegExp is present, but the dynamic pattern component is visibly generated from a hardcoded bounded numeric loop counter and limited to alphanumeric token strings, not attacker-controlled regex syntax. The additional requested context was unavailable and does not introduce any visible source that can influence the pattern, so the reported CWE-1333 ReDoS issue is not exploitable on the shown path.

## Data flow

hardcoded `0x137` argument at `vulnerabilities/javascript/source/high.js:1` → anonymous function parameter `f` at line 1 → bounded decrement loop `while(f--)` at line 1 → numeric/base conversion in `h(f)` at line 1 → alphanumeric token concatenated into `'\x5cb'+h(f)+'\x5cb'` at line 1 → sink `new RegExp(...,'g')` at line 1 → execution in `d.replace(...)` via `d[b('0x2')]` at line 1

## Answers

1. Step 0 / flagged line: The flagged line is present at `vulnerabilities/javascript/source/high.js:1`. The exact line is the single minified line beginning `var a=['fromCharCode','toString','replace','BeJ',...` and containing the specific construct `d=d[b('0x2')](new RegExp('\x5cb'+h(f)+'\x5cb','g'),g[f]);`. The reported construct is present: `new RegExp(...)` is called with a non-literal pattern built by concatenating `h(f)`.
2. Function location: The flagged construct lives inside the anonymous unpacking function passed to `eval(...)` on line 1: `eval(function(d,e,f,g,h,i){...}(b('0x7'),0x3e,0x137,b('0x8')[b('0x9')]('|'),0x0,{}));`. The provided function name is `<unknown>`; no named enclosing function is visible.
3. Step 1: The potentially dangerous RegExp pattern data originates from local hardcoded arguments on line 1, not from visible user input, file, network, or database data. Specifically, `f` is passed as the literal `0x137`, `e` is passed as `0x3e`, and `g` is produced from the hardcoded string `b('0x8')` split by `|`, all on line 1. The additional requested context for `global:a`, `global:b`, and the anonymous function was unavailable and does not change this visible source analysis.
4. Step 2: Data flow on line 1: literal `0x137` is passed to anonymous function parameter `f`; `while(f--)` iterates over this bounded counter; `h(f)` is called; `h` converts the numeric counter using modulo arithmetic, `parseInt(j/e)`, `String.fromCharCode(...)`, and `toString(0x24)`; the result is concatenated into the regex pattern `'\x5cb'+h(f)+'\x5cb'`; that pattern is passed to `new RegExp(...,'g')`; the RegExp is used in `d.replace(...)` via `d[b('0x2')]`.
5. Step 3: No explicit regex sanitizer, validation library, or ReDoS checker is visible. However, the dynamic pattern component is constrained by visible code: `h(f)` is derived from a bounded numeric loop counter initialized to hardcoded `0x137` on line 1. Its character generation is limited to numeric/base-conversion characters: values `0..35` use `toString(0x24)`, and values `36..61` use `String.fromCharCode(j+0x1d)`, producing alphanumeric token characters rather than regex metacharacters. This is sufficient to prevent attacker-controlled pathological regex construction on the flagged path.
6. Step 4: The sink is `new RegExp('\x5cb'+h(f)+'\x5cb','g')` inside `d[b('0x2')](...)`, i.e. a `replace` call, on line 1. The operation would be dangerous if the pattern were attacker-controlled or could contain catastrophic regex syntax, but the visible pattern shape is simple word-boundary matching around a generated alphanumeric token.
7. Step 5: No framework or library protection is visible or relevant. The protection is local: hardcoded inputs, bounded numeric generation, and generated alphanumeric regex tokens on line 1. The additional context returned as unavailable, so no external framework protection is relied upon.
8. Step 6: The attacker privilege/authentication state is not visible. Based on the provided code, no attacker privilege level appears relevant to the flagged RegExp pattern because no attacker-controlled input reaches the RegExp constructor.
9. Step 7: If an attacker controlled the RegExp pattern, the impact for CWE-1333 could be denial of service through catastrophic backtracking or main-thread blocking. In the shown code, that impact is not reachable because the dynamic pattern is generated from hardcoded bounded values and does not include attacker-supplied regex syntax.
10. Step 8: The weakest-looking link is the non-literal `new RegExp(...)` construction on line 1. However, for this specific finding, the defense is complete in the visible code: the only dynamic component, `h(f)`, is generated from a hardcoded bounded integer and produces simple alphanumeric tokens wrapped in `\b` word boundaries, so there is no attacker-controlled regex pattern and no visible ReDoS-enabling regex structure.

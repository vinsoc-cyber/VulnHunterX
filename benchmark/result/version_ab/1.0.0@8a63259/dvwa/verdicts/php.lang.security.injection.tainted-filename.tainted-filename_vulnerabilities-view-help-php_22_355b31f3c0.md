# php.lang.security.injection.tainted-filename.tainted-filename @ vulnerabilities/view_help.php:22

**Verdict:** FP · **Confidence:** Medium (0.76) · **Truth:** real · **Grade:** MISS · **Iterations:** 3

## Reasoning

The flagged tainted-filename construct is present and unsanitized user input reaches `file_get_contents()` at line 22, but the specific reported vulnerability is CWE-918 SSRF. In the visible code, the filename always begins with the fixed local relative prefix from line 3 and `"vulnerabilities/"` at line 22, so attacker input cannot supply the leading URL scheme required for PHP remote stream access; any remaining LFI/eval risk is outside this specific SSRF finding.

## Data flow

vulnerabilities/view_help.php:14 $_GET['id'] → $id; vulnerabilities/view_help.php:16 $_GET['locale'] → $locale; vulnerabilities/view_help.php:19 `$locale == 'en'` branch check only; vulnerabilities/view_help.php:22 fixed-prefix filename `DVWA_WEB_PAGE_TO_ROOT . "vulnerabilities/{$id}/help/help.{$locale}.php"`, with DVWA_WEB_PAGE_TO_ROOT defined as `'../'` at line 3 → file_get_contents() → eval(). Additional requested context for global:DVWA_WEB_PAGE_TO_ROOT and function:dvwaPageStartup was unavailable and adds no new transformation or sanitizer.

## Answers

1. Step 0 / flagged line location: The flagged line is line 22 in vulnerabilities/view_help.php: `eval( '?>' . file_get_contents( DVWA_WEB_PAGE_TO_ROOT . "vulnerabilities/{$id}/help/help.{$locale}.php" ) . '<?php ' );`. The construct described by the rule is present on this line: user-influenced values are interpolated into a filename argument to `file_get_contents()`. The code is top-level PHP script code; no named function is visible in lines 1-40, and the provided function label is `<unknown>`.
2. Step 1 source: The potentially dangerous data originates from HTTP query parameters in `$_GET`. Specifically, `$_GET['id']` is assigned to `$id` at line 14 and `$_GET['locale']` is assigned to `$locale` at line 16. `$_GET['security']` is also read at line 15, but it is not used in the flagged filename construction.
3. Step 2 trace: `$_GET['id']` at line 14 flows to `$id`, and `$_GET['locale']` at line 16 flows to `$locale`. At line 19, `$locale` is compared to the literal string `'en'`. If `$locale != 'en'`, execution enters the else branch at line 21. At line 22, `$id` and `$locale` are interpolated into `"vulnerabilities/{$id}/help/help.{$locale}.php"`, which is concatenated after the fixed constant `DVWA_WEB_PAGE_TO_ROOT`. The resulting filename is passed to `file_get_contents()` at line 22, and the returned contents are passed into `eval()` on the same line.
4. Step 3 validation/sanitization: Lines 11-13 only check that the `id`, `security`, and `locale` keys exist in `$_GET`; they do not validate the values. Line 19 is only a branch check for `$locale == 'en'`; it does not sanitize `$locale` in the else branch. No allowlist, basename check, path normalization, stream-wrapper blocking, or encoding is visible. The newly provided additional context is unavailable and therefore does not add any upstream sanitizer or validation.
5. Step 4 sink: The sink for this rule is `file_get_contents()` at line 22 using a filename derived from `$id` and `$locale`. The operation is dangerous for tainted filename/path handling because attacker-controlled data affects which file path is read. The same line also evaluates the file contents with `eval()`, which is independently dangerous, but the reported rule is specifically CWE-918 / tainted filename SSRF.
6. Step 5 framework/library protections: No framework or library protection is visible. `dvwaPageStartup(array('authenticated'))` is called at line 6, but its body was requested and returned unavailable, so no sanitizer can be credited from it. PHP `file_get_contents()` does not automatically validate tainted filenames. However, for the specific SSRF interpretation, the visible filename is prefixed by the fixed local string `DVWA_WEB_PAGE_TO_ROOT . "vulnerabilities/"` at line 22, with `DVWA_WEB_PAGE_TO_ROOT` visibly defined as `'../'` at line 3. This means attacker-controlled input is not at the beginning of the filename where a PHP URL scheme such as `http://` or `ftp://` would need to appear.
7. Step 6 privilege/authentication: The code calls `dvwaPageStartup(array('authenticated'))` at line 6, which visibly indicates an authenticated page startup requirement. Because the implementation of `dvwaPageStartup` is unavailable, the exact privilege level beyond authenticated user is not visible. No admin-only check appears in the provided code.
8. Step 7 impact: If the attacker controls `$id` or `$locale`, the visible code may allow unsafe local path selection and, because file contents are passed to `eval()` at line 22, could lead to severe impact if attacker-controlled PHP content can be loaded. But for the specific reported CWE-918 SSRF issue, the visible construction does not clearly allow attacker-controlled remote URLs because the argument begins with the fixed local relative prefix `../vulnerabilities/`, not with attacker input.
9. Step 8 weakest link: The weakest visible link is the lack of allowlist validation for `$id` and `$locale` before constructing a file path at line 22. That is a real weakness for file/path handling generally. For the specific SSRF claim, the visible fixed local prefix at lines 3 and 22 is a concrete defense against treating attacker input as the URL scheme, so the defense against this particular CWE-918 interpretation is sufficient in the shown path.

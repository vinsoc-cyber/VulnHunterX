# php.lang.security.injection.tainted-filename.tainted-filename @ vulnerabilities/view_help.php:20

**Verdict:** FP · **Confidence:** High (0.86) · **Truth:** real · **Grade:** MISS · **Iterations:** 3

## Reasoning

The tainted filename pattern is present, but the reported vulnerability class is CWE-918 SSRF. On the flagged path, attacker-controlled `$id` is embedded after a fixed local relative prefix and before a fixed suffix, so it cannot supply the leading `http://` or other URL wrapper scheme needed for PHP `file_get_contents(...)` to perform SSRF; the remaining local file inclusion/eval concerns are separate vulnerability classes, not this reported rule.

## Data flow

source `$_GET['id']` presence checked only at vulnerabilities/view_help.php:11 → assigned to `$id` at line 14 → branch selected by `$locale == 'en'` at line 19 → `$id` interpolated into `DVWA_WEB_PAGE_TO_ROOT . "vulnerabilities/{$id}/help/help.php"` at line 20 → constructed filename passed to `file_get_contents(...)` at line 20 → file contents passed to `eval(...)` at line 20. Additional requested context for `dvwaPageStartup`, `dvwaPageNewGrab`, `dvwaHelpHtmlEcho`, and `DVWA_WEB_PAGE_TO_ROOT` was unavailable and adds no further transformations or sanitization.

## Answers

1. Step 0 / flagged line location: The flagged line is line 20 in `vulnerabilities/view_help.php`: `eval( '?>' . file_get_contents( DVWA_WEB_PAGE_TO_ROOT . "vulnerabilities/{$id}/help/help.php" ) . '<?php ' );`. The construct described by the rule is present: `file_get_contents(...)` receives a filename/path containing `$id`, which comes from request input. This code is at PHP top level in the provided snippet; no named enclosing function is visible, and the finding lists Function as `<unknown>`.
2. Step 1: The potentially dangerous data originates from user-controlled query parameters. Specifically, `$_GET['id']` is read and assigned to `$id` on line 14 after only an existence check on line 11. `$_GET['locale']` is also read on line 16 and determines whether the flagged line 20 is reached through the branch condition on line 19.
3. Step 2: Data flow with the available context: `array_key_exists('id', $_GET)` checks only presence on line 11 → `$_GET['id']` is assigned to `$id` on line 14 → `$_GET['locale']` is assigned to `$locale` on line 16 → line 19 requires `$locale == 'en'` to enter the flagged branch → `$id` is interpolated into the path string `DVWA_WEB_PAGE_TO_ROOT . "vulnerabilities/{$id}/help/help.php"` on line 20 → that constructed path is passed to `file_get_contents(...)` on line 20 → returned contents are concatenated into `eval(...)` on line 20. The requested additional contexts for `dvwaPageStartup`, `dvwaPageNewGrab`, `dvwaHelpHtmlEcho`, and global `DVWA_WEB_PAGE_TO_ROOT` were unavailable, so they do not change the visible trace.
4. Step 3: No validation, sanitization, canonicalization, allowlist, or encoding for `$id` is visible before line 20. Lines 11-13 only check that the query keys exist. The comparison `$locale == 'en'` on line 19 selects the flagged branch but does not sanitize `$id`. No additional sanitizer was revealed by the supplied context because the requested functions/globals were unavailable.
5. Step 4: The sink for this specific Semgrep rule is `file_get_contents(...)` on line 20, where an attacker-influenced value is used in a filename/path. The dangerous operation claimed by the rule is tainted filename use that may lead to SSRF when PHP file APIs are given attacker-controlled URLs. The same line also uses `eval(...)` on file contents, but that is a separate issue from the reported CWE-918 SSRF rule.
6. Step 5: No framework or library automatic protection is visible. `dvwaPageStartup(array('authenticated'))` is called on line 6, but its body was unavailable, so no request-parameter validation or filename protection can be credited from it. However, for the specific SSRF claim, the visible path construction itself places a fixed relative prefix before `$id`: `DVWA_WEB_PAGE_TO_ROOT . "vulnerabilities/"` on line 20. In PHP, URL stream wrappers such as `http://` must be at the start of the filename string to be interpreted as remote URLs; here the attacker-controlled `$id` is not at the start of the string.
7. Step 6: The visible code suggests the attacker must be authenticated because `dvwaPageStartup(array('authenticated'))` is called on line 6 before the GET parameters are used. No admin-only requirement is visible. Because the body of `dvwaPageStartup` is unavailable, the exact authentication enforcement details are not visible in provided context.
8. Step 7: If an attacker controls `$id`, the visible concrete risks include attacker-influenced local path selection and, because the resulting file contents are passed to `eval(...)` on line 20, possible code execution if the attacker can cause a chosen local file containing PHP code to be read. For the specific reported CWE-918 SSRF issue, the visible code does not clearly permit remote URL fetching because `$id` is embedded after a fixed local prefix and before a fixed suffix rather than controlling the full filename from the beginning.
9. Step 8: The weakest visible defense is the lack of validation or allowlisting of `$id` before it is used in a file path and evaluated on line 20. However, for the specific reported SSRF/tainted-filename class, the defense against SSRF is the fixed non-user-controlled prefix before `$id` on line 20, which prevents the attacker-controlled value from becoming the leading URL scheme of the filename. No newly provided context changes this conclusion because the requested context was unavailable.

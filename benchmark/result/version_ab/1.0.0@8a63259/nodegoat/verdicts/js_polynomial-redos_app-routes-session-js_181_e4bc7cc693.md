# js/polynomial-redos @ app/routes/session.js:181

**Verdict:** TP · **Confidence:** Low (0.55) · **Truth:** real · **Grade:** CORRECT · **Iterations:** 6

## Reasoning

The flagged line contains the vulnerable regex operation, and untrusted request-body `email` reaches it without any visible length bound or ReDoS mitigation. Although route registration and body-parser limits were not found, no specific defense is visible; under the required final balance-of-evidence standard for untrusted input with no bounds checking, this leans True Positive.

## Answers

1. Step 0 / flagged line: The exact flagged line is app/routes/session.js:181: `if (!EMAIL_RE.test(email)) {`. It lives inside the nested `validateSignup` function, defined at app/routes/session.js:138 within `SessionHandler`. The `js/polynomial-redos` construct is present on that line: a JavaScript regular expression test is performed on variable input `email` using `EMAIL_RE`, declared at app/routes/session.js:143 as `/^[\S]+@[\S]+\.[\S]+$/`.
2. Step 1 / source: The dangerous data originates from `req.body` in `handleSignup`. In app/routes/session.js:191-198, `req.body` is destructured, and `email` is extracted at line 192. This is request body data and is treated as attacker-controlled input for this analysis.
3. Step 2 / trace: The complete visible data flow is: app/routes/session.js:191-198 destructures `req.body` → app/routes/session.js:192 assigns request field `email` → app/routes/session.js:203 also stores `email` into the `errors` object, though that copy is not necessary for the regex sink → app/routes/session.js:206 passes `email` to `validateSignup(userName, firstName, lastName, password, verify, email, errors)` → app/routes/session.js:138 receives it as the `email` parameter → app/routes/session.js:180 checks only whether it is non-empty → app/routes/session.js:181 evaluates `EMAIL_RE.test(email)`.
4. Step 3 / validation/sanitization: There is no sufficient ReDoS mitigation visible for `email`. Other fields have maximum-length regex validations: `USER_RE = /^.{1,20}$/` at line 140 tested at line 159, `FNAME_RE = /^.{1,100}$/` at line 141 tested at line 163, `LNAME_RE = /^.{1,100}$/` at line 142 tested at line 167, and `PASS_RE = /^.{1,20}$/` at line 144 tested at line 171. By contrast, `email` uses `EMAIL_RE = /^[\S]+@[\S]+\.[\S]+$/` at line 143, and before the sink there is only `if (email !== "")` at line 180. No maximum email length check, regex timeout, safe-regex library, or linear-time parser is visible.
5. Step 4 / sink: The sink is app/routes/session.js:181: `if (!EMAIL_RE.test(email)) {`. The unsafe operation is evaluating the backtracking JavaScript regex `/^[\S]+@[\S]+\.[\S]+$/` from line 143 against attacker-controlled `email`. CodeQL specifically reports polynomial slow cases for strings with repeated tab/at or tab/dot sequences, so crafted long non-matching input can cause excessive CPU work.
6. Step 5 / framework/library protections: No framework or library protection is visible. Additional context reported no `global:app`, `global:router`, or `global:bodyParser`, and no `module.exports`/`exports`. No route middleware, request body size limit, email field length limit, regex timeout, or automatic validation layer is shown. Therefore there is no specific visible defense to support a False Positive verdict.
7. Step 6 / privilege/authentication: The sink is reached from `this.handleSignup` at app/routes/session.js:189-254. No authentication or admin check appears inside `handleSignup` before `validateSignup` is called at line 206. Although no callers for `handleSignup` or `SessionHandler` were found in the provided context, the function is a signup handler and directly consumes `req.body`; on the required final balance-of-evidence standard, this leans toward externally reachable/untrusted request handling rather than safe or dead code.
8. Step 7 / impact: The concrete security impact is denial of service / resource exhaustion. An attacker controlling `req.body.email` can submit a crafted long string that causes slow polynomial-time regular expression evaluation at app/routes/session.js:181, tying up the Node.js event loop CPU.
9. Step 8 / weakest link: The weakest link is the missing length bound or safe email validation before the regex sink. The code demonstrates that other fields are bounded but email is not, and no upstream framework protection was found in the additional context. Because no specific visible defense prevents exploitation, the balance of evidence favors a real ReDoS issue.

# php.lang.security.injection.tainted-sql-string.tainted-sql-string @ vulnerabilities/bac/source/medium.php:71

**Verdict:** TP · **Confidence:** High (0.9) · **Truth:** real · **Grade:** CORRECT · **Iterations:** 3

## Reasoning

The additional requested context was unavailable and does not reveal any mitigating defense. In the visible code, attacker-controlled `HTTP_X_FORWARDED_FOR` flows directly into a manually constructed SQL string on lines 71-72 and is executed with raw `mysqli_query` on line 73 without escaping or parameterization.

## Data flow

vulnerabilities/bac/source/medium.php:69 `$_SERVER['HTTP_X_FORWARDED_FOR']` / `$_SERVER['REMOTE_ADDR']` → `$ip` assigned on line 69 → `$ip` interpolated into `$log_query` on lines 71-72 → `$log_query` executed by `mysqli_query($GLOBALS["___mysqli_ston"], $log_query)` on line 73. Additional visible related flow: `$id` → `$check_query` on line 21 → `mysqli_query` on line 22 → `$user_exists` on line 23 → `$target_id = $user_exists ? $id : 0` on line 70 → `$log_query` on lines 71-72 → `mysqli_query` on line 73.

## Answers

1. Step 0 / flagged line location: The flagged line 71 is present and reads exactly: `$log_query = "INSERT INTO bac_log (user_id, target_id, ip_address) VALUES `. It lives in function `<unknown>` according to the provided context; the requested `function:<unknown>` context was unavailable. The SQL string continues on line 72 with interpolated values: `({$current_user_id}, {$target_id}, '{$ip}')`, confirming the manually constructed SQL string construct is present at the flagged location.
2. Step 1: The dangerous visible source is `$_SERVER['HTTP_X_FORWARDED_FOR']` on line 69, assigned to `$ip` if present. This HTTP header is client-controllable in typical PHP deployments. The fallback `$_SERVER['REMOTE_ADDR']` is less directly attacker-controlled, but because `HTTP_X_FORWARDED_FOR` is preferred whenever set, the attacker-controlled source is visible. The additional global contexts for `_SERVER`, `_GET`, and `___mysqli_ston` were unavailable and do not change this visible source analysis.
3. Step 2: Data flow: `$_SERVER['HTTP_X_FORWARDED_FOR']` or `$_SERVER['REMOTE_ADDR']` is assigned to `$ip` on line 69. `$target_id` is assigned from `$id` if `$user_exists` is true, otherwise `0`, on line 70. `$ip`, `$target_id`, and `$current_user_id` are interpolated into `$log_query` on lines 71-72. `$log_query` is then executed by `mysqli_query($GLOBALS["___mysqli_ston"], $log_query)` on line 73. Separately, `$id` is also used earlier in `$check_query` on line 21, executed on line 22, and the result determines `$user_exists` on line 23.
4. Step 3: No validation, sanitization, SQL escaping, encoding, casting, or prepared-statement binding is visible for `$ip` between its assignment on line 69 and SQL interpolation on lines 71-72. No adequate sanitization is visible for `$target_id` or `$current_user_id` either. The comment on line 68 says only numeric target IDs are logged, but line 70 does not cast or validate `$id`; comments are not defenses. The newly provided context is unavailable and therefore shows no additional defense.
5. Step 4: The sink is `mysqli_query($GLOBALS["___mysqli_ston"], $log_query)` on line 73. The unsafe operation is execution of a manually constructed SQL string from lines 71-72 where `$ip` is inserted inside SQL quotes without escaping or parameterization.
6. Step 5: No framework or library automatic protection is visible. The code uses raw `mysqli_query` on line 73, not a prepared statement, ORM, or parameterized API. The requested `global:___mysqli_ston` context was unavailable, but the visible call to `mysqli_query` with a complete SQL string does not provide automatic parameter binding.
7. Step 6: The exact authentication or privilege level required is not fully visible because the enclosing function/caller context remains unavailable. Lines 26-47 show a weak token check for profile display, but the logging block on lines 49-73 appears outside that inner profile-display branch. Regardless, the SQL injection sink is reachable along the visible path whenever this code block executes and the attacker can supply `HTTP_X_FORWARDED_FOR`.
8. Step 7: If an attacker controls `$ip`, they can inject SQL into the `INSERT INTO bac_log` statement. Concrete impact includes log tampering and unauthorized database manipulation; depending on database permissions and SQL behavior, it may also enable data theft or denial of service. The vulnerability class is SQL injection, CWE-89.
9. Step 8: The weakest link is the direct interpolation of `$ip` from `$_SERVER['HTTP_X_FORWARDED_FOR']` into the SQL string on lines 71-72, followed by raw execution on line 73. No visible defense completes the chain: there is no prepared statement, no `mysqli_real_escape_string`, and no strict allowlist/validation for the header value.

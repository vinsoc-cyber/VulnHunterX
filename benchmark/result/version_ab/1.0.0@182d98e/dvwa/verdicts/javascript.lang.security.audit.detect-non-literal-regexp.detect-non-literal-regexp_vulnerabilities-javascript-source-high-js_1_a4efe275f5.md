# javascript.lang.security.audit.detect-non-literal-regexp.detect-non-literal-regexp @ vulnerabilities/javascript/source/high.js:1

**Verdict:** FP · **Confidence:** High (0.9) · **Truth:** not-real · **Grade:** CORRECT · **Iterations:** 3

## Reasoning

The flagged non-literal `RegExp()` call is real, but the pattern component reaching it is generated from a constant bounded loop counter, not attacker-controlled data. Because `h(f)` produces generated alphanumeric tokens and the final regex is only a simple word-boundary match, the CWE-1333/ReDoS condition is not exploitable on the shown path.

## Data flow

constant `0x137` at vulnerabilities/javascript/source/high.js:1 → parameter `f` of `function(d,e,f,g,h,i)` at line 1 → loop `while(f--)` at line 1 → local encoder `h(f)` at line 1 → concatenation into `'\x5cb'+h(f)+'\x5cb'` at line 1 → sink `new RegExp(...,'g')` at line 1 → use in `replace` via `d[b('0x2')](..., g[f])` at line 1. Additional requested context was unavailable and adds no contrary source or transformation.

## Answers

1. Step 0 / Locate flagged line: The flagged line is present at `vulnerabilities/javascript/source/high.js:1`, in the top-level script / `<unknown>` function context. The line is the single minified line beginning `var a=['fromCharCode','toString','replace','BeJ',...` and it contains the exact flagged sink text `new RegExp('\x5cb'+h(f)+'\x5cb','g')` inside `d=d[b('0x2')](new RegExp('\x5cb'+h(f)+'\x5cb','g'),g[f]);`. The construct described by the rule, a non-literal `RegExp()` call, is present on that line.
2. Q1: The vulnerability class from the rule is CWE-1333 / Regular Expression Denial of Service caused by dynamically constructing a regex pattern. The potentially dangerous regex data in this specific sink originates from `h(f)` on line 1. In the visible code, `f` is supplied as the constant `0x137` to the unpacking function call on line 1, not from user input, file, network, database, or another attacker-controlled source. The additional requested contexts for `global:a`, `global:b`, enclosing wrapper, generated eval code, and callers are unavailable and do not add a visible attacker-controlled source.
3. Q2: Data flow with all available context: on line 1, the code calls `eval(function(d,e,f,g,h,i){...}(b('0x7'),0x3e,0x137,b('0x8')[b('0x9')]('|'),0x0,{}));`; the third argument `0x137` becomes local parameter `f`; local `h` is assigned `function(j){return ...}` on line 1; `while(f--)` iterates over bounded numeric values; `h(f)` is concatenated into `'\x5cb'+h(f)+'\x5cb'`; that string becomes the first argument to `new RegExp(...,'g')`; the resulting regex is used by `d[b('0x2')](..., g[f])`, where `b('0x2')` resolves to `replace` from the static array at line 1.
4. Q3: No explicit regex sanitizer or validation library is visible. However, a specific visible defense exists for this flagged sink: the regex component is generated from a bounded numeric loop counter initialized from constant `0x137` on line 1. The function `h(j)` converts numeric values using base conversion / `toString(0x24)` and `String.fromCharCode(...)` on line 1, producing identifier-like alphanumeric tokens rather than attacker-supplied regex syntax. This is sufficient for this specific sink because no user-controlled regex metacharacters can reach `RegExp()` through the shown path.
5. Q4: The sink is `new RegExp('\x5cb'+h(f)+'\x5cb','g')` at `vulnerabilities/javascript/source/high.js:1`. The operation would be dangerous if attacker-controlled data could affect the regex pattern, because dynamic regex construction can create catastrophic backtracking. Here, the actual generated pattern is a word-boundary pattern around a generated token, not an attacker-provided pattern.
6. Q5: No framework or library automatic protection is visible or relevant. The protection is not framework-based; it is the visible constant-derived and bounded construction of the regex argument on line 1.
7. Q6: The privilege or authentication state needed to execute the script is not visible in the provided context. The additional caller / script-loader context is unavailable. For the flagged sink itself, this does not change the analysis because the regex argument is not derived from attacker-controlled input in the visible code.
8. Q7: If an attacker controlled the `RegExp()` pattern, the impact could be client-side or server-side JavaScript ReDoS by blocking the event loop / main thread. In this instance, the provided code does not show attacker control of the pattern, and the pattern fragment is generated from a bounded constant numeric loop.
9. Q8: No weak link exists for the flagged ReDoS data path in the visible code. The defense chain is complete for this sink because the non-literal part of the regex is generated internally from a constant bounded number and converted to alphanumeric text before being wrapped with `\b` boundaries.

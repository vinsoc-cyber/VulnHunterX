# javascript.lang.security.audit.detect-non-literal-regexp.detect-non-literal-regexp @ vulnerabilities/javascript/source/high.js:1

**Verdict:** FP · **Confidence:** High (0.92) · **Truth:** not-real · **Grade:** CORRECT · **Iterations:** 3

## Reasoning

The flagged non-literal RegExp sink is present, but the dynamic regex component is visibly derived from constants and a bounded numeric loop counter, not from attacker-controlled input. The additional requested context is unavailable and does not introduce any source that could make the RegExp pattern attacker-controlled, so CWE-1333/ReDoS is not exploitable on the shown path.

## Data flow

vulnerabilities/javascript/source/high.js:1 local literal array `a` → decoder `b(...)` returns literal table entries → IIFE called with constant `f=0x137` and constant `e=0x3e` → `while(f--)` bounds/decrements numeric `f` → `h(f)` deterministically converts the number to token characters → `new RegExp('\x5cb'+h(f)+'\x5cb','g')` → replace operation `d[b('0x2')](..., g[f])`

## Answers

1. Step 0 / flagged line: The flagged line is visible at vulnerabilities/javascript/source/high.js:1. It is a single long top-level JavaScript line beginning `var a=['fromCharCode','toString','replace',...` and containing the exact flagged construct `d=d[b('0x2')](new RegExp('\x5cb'+h(f)+'\x5cb','g'),g[f]);`. This confirms the rule-described construct is present on the flagged line: `new RegExp(...)` is called with a non-literal concatenated argument.
2. Step 1 / source: The potentially dangerous RegExp component is `h(f)` at line 1. In the visible code, `f` originates from the constant argument `0x137` in the immediate function invocation: `}(b('0x7'),0x3e,0x137,b('0x8')[b('0x9')]('|'),0x0,{}));` on line 1. The additional requested context for the anonymous function and globals `a`/`b` is unavailable and does not add evidence of user input. No HTTP request, file, network, database, DOM input, or other attacker-controlled source is visible.
3. Step 2 / trace: At line 1, array `a` is initialized with literal strings. Line 1 defines `b=function(c,d){c=c-0x0;var e=a[c];return e;};`, which indexes that local literal array. Line 1 invokes `eval(function(d,e,f,g,h,i){...}(b('0x7'),0x3e,0x137,b('0x8')[b('0x9')]('|'),0x0,{}));`. Inside that function on line 1, `h=function(j){return(j<e?'':h(parseInt(j/e)))+((j=j%e)>0x23?String[b('0x0')](j+0x1d):j[b('0x1')](0x24));};` transforms numeric `f` into token strings. Then `while(f--)` iterates over bounded numeric values, and line 1 uses `h(f)` in `new RegExp('\x5cb'+h(f)+'\x5cb','g')`.
4. Step 3 / validation/sanitization: No explicit regex sanitizer or ReDoS-checking library is visible. However, the visible code provides a concrete non-taint defense: the dynamic regex text is derived from a bounded numeric constant `0x137` and converted by `h` into base-style tokens using `toString(0x24)` or `String.fromCharCode(j+0x1d)` on line 1. Since `e` is the constant `0x3e`, the modulo value is bounded and produces token characters rather than attacker-controlled regex syntax.
5. Step 4 / sink: The sink is `new RegExp('\x5cb'+h(f)+'\x5cb','g')` at line 1, immediately used as the pattern argument to a replace call: `d=d[b('0x2')](new RegExp('\x5cb'+h(f)+'\x5cb','g'),g[f]);`. The dangerous operation would be compiling and running an attacker-controlled regular expression, potentially causing ReDoS. In this path, the pattern is generated from constants and numeric loop state.
6. Step 5 / framework/library protections: No framework or library protection is visible or relevant. There is no ORM, template engine, regex validation library, or framework guard shown. The safety conclusion rests on the visible local constant derivation of the regex argument on line 1.
7. Step 6 / privilege/authentication: Not visible in provided context. The snippet is top-level JavaScript, and no route, request handler, authentication check, or caller is shown. The additional caller/global context provided is unavailable, so authentication state cannot be determined from the snippet.
8. Step 7 / impact if attacker controlled data: If an attacker controlled the RegExp pattern argument, the impact would be denial of service via a pathological regular expression blocking JavaScript execution. But the provided code does not show attacker control over `h(f)`, `f`, `e`, or the regex pattern; they are initialized from literals/constants on line 1.
9. Step 8 / weakest link: No exploitable weak link is visible for this specific flagged sink. The defense chain is complete in the shown path because the dynamic RegExp argument comes from a bounded constant loop counter and deterministic token-generation function on line 1, not from external input. The unavailable additional context does not change that visible data flow.

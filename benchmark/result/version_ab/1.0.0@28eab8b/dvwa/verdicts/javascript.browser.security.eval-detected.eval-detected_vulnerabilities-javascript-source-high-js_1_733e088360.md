# javascript.browser.security.eval-detected.eval-detected @ vulnerabilities/javascript/source/high.js:1

**Verdict:** FP · **Confidence:** High (0.88) · **Truth:** not-real · **Grade:** CORRECT · **Iterations:** 3

## Reasoning

The flagged `eval(...)` sink is real and present on line 1, but the data passed to it is visibly derived from hardcoded literals through deterministic obfuscation/unpacking on the same line. No external attacker-controlled source is visible in the original code or the additional context, so this is unsafe style but not a demonstrated exploitable CWE-95 code-injection path.

## Data flow

hardcoded array literal `var a=[...]` (line 1) → in-place array rotation via `(function(c,d){...}(a,0x1f4))` using `push`/`shift` (line 1) → local lookup helper `var b=function(c,d){... return a[c]; }` (line 1) → packed string `b('0x7')` and token list `b('0x8')[b('0x9')]('|')` (line 1) → unpacking function performs `replace(new RegExp(...), ...)` and returns `d` (line 1) → `eval(...)` executes the returned string (line 1). Additional contexts `global:a`, `global:b`, and loader body were unavailable and add no visible external source.

## Answers

1. Step 0: The flagged line is present at line 1 in Function `<unknown>`. The exact relevant flagged text is `eval(function(d,e,f,g,h,i){h=function(j){return(j<e?'':h(parseInt(j/e)))+((j=j%e)>0x23?String[b('0x0')](j+0x1d):j[b('0x1')](0x24));};if(!''[b('0x2')](/^/,String)){while(f--){i[h(f)]=g[f]||h(f);}g=[function(k){if('wpA'!==b('0x3')){return i[k];}else{while(f--){i[k(f)]=g[f]||k(f);}g=[function(l){return i[l];}];k=function(){return b('0x4');};f=0x1;}}];h=function(){return b('0x4');};f=0x1;};while(f--){if(g[f]){if(b('0x5')===b('0x6')){return i[h];}else{d=d[b('0x2')](new RegExp('\x5cb'+h(f)+'\x5cb','g'),g[f]);}}}return d;}(b('0x7'),0x3e,0x137,b('0x8')[b('0x9')]('|'),0x0,{}));`. The construct described by the rule, `eval(...)`, is present on the flagged line.
2. Q1: The potentially dangerous data originates from hardcoded string/array literals in `var a=[...]` on line 1. The additional requested contexts for `global:a` and `global:b` were unavailable, but the visible code declares and initializes `var a` and `var b` on the same line before the `eval(...)` call. No user input, HTTP request, file, network data, database value, DOM input, or other external source is visible.
3. Q2: Data trace with additional context considered: line 1 initializes `a` with hardcoded literals; line 1 runs `(function(c,d){...}(a,0x1f4))`, which mutates `a` using `push` and `shift`; line 1 defines `b=function(c,d){ c=c-0x0; var e=a[c]; return e; }`; line 1 calls `b('0x7')` to obtain the packed code string; line 1 calls `b('0x8')[b('0x9')]('|')` to obtain a token array using `split('|')`; line 1 passes those values into the unpacking function; line 1 performs deterministic token replacement with `replace(new RegExp(...), ...)`; line 1 returns `d` and passes it directly to `eval(...)`. The added context is unavailable and does not show any external write to `a` or `b`.
4. Q3: No validation, sanitization, or encoding is applied before the `eval(...)` sink on line 1. The operations visible on line 1 are obfuscation/unpacking transformations, including array rotation, lookup, `split`, `RegExp`, and `replace`. These are not sufficient sanitizers for code injection if the input were attacker-controlled. However, the visible source data is hardcoded, not attacker-controlled.
5. Q4: The sink is `eval(...)` on line 1. The dangerous operation is dynamic JavaScript code execution of the unpacked string returned by the immediately invoked function.
6. Q5: No framework or library automatic protections are visible. This is native JavaScript `eval` on line 1, with no visible CSP enforcement, template auto-escaping, sandboxing, or other framework-level protection.
7. Q6: The code appears to run at top level when the script is loaded. No authentication or privilege guard is visible in the provided context. A page visitor could trigger execution by loading the script, but the visible code does not show a way for that visitor to control the evaluated string.
8. Q7: If an attacker controlled the string passed to `eval(...)`, the impact would be arbitrary JavaScript execution in the browser context, such as XSS, token/session theft, DOM manipulation, phishing, or actions as the victim. In the provided code and added context, attacker control over the evaluated string is not shown.
9. Q8: The weakest link is the use of `eval(...)` on line 1, because it executes dynamically generated code and would be exploitable if external input reached it. For this specific finding, the visible defense is that the evaluated payload is built from hardcoded literals declared in the same statement and transformed deterministically before execution; the additional context did not provide evidence of any external source reaching the sink.

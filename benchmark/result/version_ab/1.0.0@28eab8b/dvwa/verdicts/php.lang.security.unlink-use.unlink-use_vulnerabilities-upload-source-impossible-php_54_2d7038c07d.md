# php.lang.security.unlink-use.unlink-use @ vulnerabilities/upload/source/impossible.php:54

**Verdict:** FP · **Confidence:** High (0.9) · **Truth:** not-real · **Grade:** CORRECT · **Iterations:** 3

## Reasoning

The additional context was unavailable and does not change the visible data flow. The flagged `unlink($temp_file)` sink is present, but the only user-derived part of `$temp_file` is the extension, which is explicitly whitelisted on line 26, while the filename basename is generated from `random_bytes(16)` on line 19; therefore no attacker-controlled path traversal or arbitrary deletion target reaches the sink.

## Data flow

Additional context supplied no new executable code and does not change the trace: vulnerabilities/upload/source/impossible.php line 8 `$_FILES['uploaded']['name']` → line 9 `$uploaded_ext` via `substr()`/`strrpos()` → line 26 extension whitelist `jpg|jpeg|png` → line 19 `$random_name = bin2hex(random_bytes(16)) . '.' . $uploaded_ext` → line 22 temp directory from `ini_get('upload_tmp_dir')` or `sys_get_temp_dir()` → line 23 `$temp_file` path assembly → lines 34/38 write re-encoded image to `$temp_file` → line 43 `rename($temp_file, ...)` → line 53 `file_exists($temp_file)` → line 54 `unlink( $temp_file );`

## Answers

1. Step 0 / Flagged line location: The flagged line is present at line 54 and is exactly `unlink( $temp_file );`. This is the dangerous construct described by the rule: PHP `unlink()` is being called on a variable path. The enclosing function name is not visible; the snippet labels it as `Function: <unknown>`.
2. Step 1 / Source: The potentially dangerous data originates from the HTTP file upload array `$_FILES['uploaded']`. Specifically, `$_FILES['uploaded']['name']` is read into `$uploaded_name` on line 8, and the derived extension contributes to `$temp_file`. Other request-derived upload fields are read on lines 10-12, but the path passed to `unlink()` is built from the uploaded filename extension plus server-side temp directory/random name.
3. Step 2 / Trace: `$_FILES['uploaded']['name']` line 8 → `$uploaded_ext = substr($uploaded_name, strrpos($uploaded_name, '.') + 1)` line 9 → `$random_name = bin2hex(random_bytes(16)) . '.' . $uploaded_ext` line 19 → `$temp_file` is set to either `ini_get('upload_tmp_dir')` or `sys_get_temp_dir()` line 22 → `$temp_file .= DIRECTORY_SEPARATOR . $random_name` line 23 → temporary image is written to `$temp_file` by `imagejpeg()` on line 34 or `imagepng()` on line 38 → `rename($temp_file, ...)` line 43 → `file_exists($temp_file)` line 53 → `unlink($temp_file)` line 54.
4. Step 3 / Validation, sanitization, encoding: The visible code validates the user-controlled extension before the unlink path can execute. Line 26 requires `strtolower($uploaded_ext)` to be exactly `jpg`, `jpeg`, or `png`. Lines 27-29 also require size under 100000, MIME type `image/jpeg` or `image/png`, and `getimagesize($uploaded_tmp)`. For CWE-22/path traversal in the flagged `unlink()` path, the critical defense is that the attacker cannot control the basename because it is generated by `random_bytes(16)` on line 19, and the only attacker-derived suffix is restricted to the extension whitelist on line 26. This prevents path separators or traversal payloads from being included in the path reaching `unlink()`.
5. Step 4 / Sink: The sink is line 54, `unlink( $temp_file );`. The dangerous operation is deletion of a filesystem path. If `$temp_file` were attacker-controlled, this could cause arbitrary file deletion, but the shown path constrains the path to a temp directory plus a random filename and whitelisted image extension.
6. Step 5 / Framework or library protections: The additional context did not provide any framework-level protection details. PHP `unlink()` itself does not protect against malicious paths. `checkToken()` on line 5 is unavailable and, in any case, CSRF protection would not sanitize filesystem paths. The effective visible protection is application-level path construction: random filename generation on line 19 plus extension whitelist on line 26.
7. Step 6 / Privilege or authentication state: Not visible in provided context. The code uses `$_REQUEST['user_token']` and `$_SESSION['session_token']` on line 5, suggesting a session/CSRF workflow, but the authentication or authorization level required to reach this code is not shown. The scanner/source context and use of `$_FILES` indicate externally supplied upload data can reach the handler.
8. Step 7 / Concrete security impact if attacker controlled the data: If an attacker could control `$temp_file`, the impact would be arbitrary file deletion within the permissions of the PHP process, potentially denial of service or deletion of application/user files. In the provided code, however, attacker control over the unlink target is blocked by the random basename and whitelisted extension.
9. Step 8 / Weakest link: The weakest link is the use of user-derived `$uploaded_ext` in `$random_name` on line 19. The defense is complete for the flagged CWE-22 unlink path because line 26 restricts that extension to `jpg`, `jpeg`, or `png`, while the basename is generated by `bin2hex(random_bytes(16))`, so the attacker cannot inject `../`, path separators, or choose a victim filename.

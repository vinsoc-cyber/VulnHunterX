# yaml.github-actions.security.run-shell-injection.run-shell-injection @ .github/workflows/docker-image.yml:29

**Verdict:** TP · **Confidence:** Medium (0.78) · **Truth:** not-real · **Grade:** FALSE-ALARM · **Iterations:** 3

## Reasoning

The flagged `run:` block directly interpolates `github.ref` into shell commands on lines 33 and 34 without shell-safe indirection or escaping, and the later `sed` processing occurs too late to prevent command substitution. The unavailable permissions and branch-protection context affects attacker privilege assessment, but it does not provide a visible defense against the command-injection sink.

## Data flow

GitHub Actions workflow trigger (`workflow_dispatch` line 4 / `push` to `master` lines 5-7) → GitHub context source `${{ github.ref }}` lines 33-34 → direct expression interpolation into Bash script under `run: |` at line 29 → shell evaluation in `VERSION=$(echo "${{ github.ref }}" | sed ...)` line 33 and `[[ "${{ github.ref }}" == "refs/tags/"* ]]` line 34 → derived `VERSION` used in `docker buildx build --tag $IMAGE_ID:$VERSION` line 42

## Answers

1. Step 0 / flagged line location: The flagged line is `.github/workflows/docker-image.yml:29`, exact text: `run: |`. It is not inside a PHP function; it lives in the GitHub Actions workflow job `build`, in the step named `Build and push multi-arch DVWA image` at line 28. The dangerous construct described by the rule is present in this `run:` block via direct `${{ github.* }}` interpolation on lines 31, 33, 34, and 36.
2. Step 1: The potentially dangerous data originates from the GitHub Actions `github` context: `${{ github.repository_owner }}` on line 31, `${{ github.ref }}` on lines 33 and 34, and `${{ github.sha }}` on line 36. The most relevant attacker-influenced source is `github.ref` because ref names, especially branch or tag names used with `workflow_dispatch`, can contain shell-significant characters such as command substitution syntax.
3. Step 2: Data flow with additional context included: workflow triggers are `workflow_dispatch` on line 4 and `push` to `master` on lines 5-7. The job starts at lines 9-11. In the flagged shell block beginning line 29, `github.repository_owner` flows into `IMAGE_ID=ghcr.io/${{ github.repository_owner }}/dvwa` on line 31, then through `echo $IMAGE_ID | tr '[A-Z]' '[a-z]'` on line 32, then into Docker tags on lines 42-43. `github.ref` flows into `VERSION=$(echo "${{ github.ref }}" | sed -e 's,.*/\(.*\),\1,')` on line 33, then into the conditional `[[ "${{ github.ref }}" == "refs/tags/"* ]]` on line 34, then into `VERSION`, which is used in the Docker tag on line 42. `github.sha` flows into `COMMIT=$(echo "${{ github.sha }}" | cut -c 1-7)` on line 36, then into the Docker tag on line 43.
4. Step 3: No sufficient validation, sanitization, or shell encoding is visible. The `sed` operations on lines 33-34, `tr` on line 32, and `cut` on line 36 happen after GitHub expression interpolation has already inserted the values into the shell script. Double quotes around `${{ github.ref }}` on lines 33 and 34 are not sufficient to prevent shell command substitution, because after interpolation a value such as `refs/heads/$(cmd)` inside double quotes can still be evaluated by the shell.
5. Step 4: The sink is the shell execution context introduced by `run: |` at line 29. The dangerous operation is direct insertion of GitHub context values into shell commands on lines 31, 33, 34, and 36. The highest-risk sink usage is `${{ github.ref }}` on lines 33 and 34 because it is evaluated as part of a Bash script rather than passed through a safe environment variable boundary.
6. Step 5: No automatic framework or library protection is visible. GitHub Actions expression interpolation does not shell-escape `${{ ... }}` values before placing them into a `run:` script. The additional requested context for `global:permissions` and `global:repository_settings_branch_protection` is unavailable, so there is no visible repository-level mitigation. A safer pattern would be assigning the GitHub context value to `env:` and then referencing it as a quoted shell variable such as `"$REF"`; that protection is not present.
7. Step 6: The visible workflow can be triggered by `workflow_dispatch` on line 4 or by pushes to `master` on lines 5-7. For the `push` trigger restricted to `master`, an attacker would likely need permission to push to `master`, which is not visible. For `workflow_dispatch`, an attacker would need the ability to manually trigger the workflow and influence the selected ref; the exact GitHub permission requirements and branch protection configuration are not visible because the requested global context was unavailable.
8. Step 7: If an attacker controls the interpolated `github.ref`, the concrete impact is command injection / RCE in the GitHub Actions runner. The workflow logs into GitHub Container Registry using `${{ secrets.GITHUB_TOKEN }}` on lines 21-26 and performs Docker build and push operations on lines 41-44, so injected commands could steal job-accessible tokens, access source code, or tamper with published container images.
9. Step 8: The weakest link is direct use of `${{ github.ref }}` inside the shell script on lines 33 and 34. There is no visible complete defense: post-interpolation text processing with `sed` does not prevent shell parsing, GitHub Actions does not auto-shell-escape the expression, and the requested permission/branch-protection context is unavailable.

# php.lang.security.php-permissive-cors.php-permissive-cors @ vulnerabilities/api/gen_openapi.php:6

**Verdict:** TP · **Confidence:** Medium (0.74) · **Truth:** not-real · **Grade:** FALSE-ALARM · **Iterations:** 5

## Reasoning

The flagged sink is present and unconditionally emits `Access-Control-Allow-Origin: *` on line 6 with no visible origin validation or compensating control. When this PHP script is reachable, arbitrary origins can read the generated OpenAPI response, making the permissive CORS finding valid based on the available code.

## Data flow

hardcoded literal `Access-Control-Allow-Origin: *` (line 6) → PHP `header()` call (line 6) → browser receives wildcard CORS policy allowing any origin to read the response; response body is generated by `\OpenApi\Generator::scan(['./src'])` (line 4) → emitted via `$openapi->toYaml()` (line 12)

## Answers

1. Step 0 / locate flagged line: The flagged line is present at line 6 and reads exactly: `header("Access-Control-Allow-Origin: *");`. This confirms the rule-described construct is present: PHP emits an `Access-Control-Allow-Origin` response header with wildcard `*`.
2. Step 1 / source: The dangerous value originates from a hardcoded string literal on line 6: `Access-Control-Allow-Origin: *`. This is not attacker-controlled input; the issue is an insecure CORS configuration.
3. Step 2 / data trace: The literal string `Access-Control-Allow-Origin: *` flows directly into PHP's `header()` function on line 6. There are no intermediate variables or transformations. Separately, the response body is generated by `$openapi = \OpenApi\Generator::scan(['./src']);` on line 4 and emitted with `echo $openapi->toYaml();` on line 12.
4. Step 3 / validation, sanitization, or encoding: No validation, sanitization, encoding, trusted-origin allowlist, conditional check, or environment guard is visible before the header is emitted on line 6. The unavailable additional context does not provide any visible defense.
5. Step 4 / sink: The sink is the PHP `header()` call on line 6. The unsafe operation is sending `Access-Control-Allow-Origin: *`, which instructs browsers to allow any origin to read this endpoint's response through CORS, subject to browser credential restrictions.
6. Step 5 / framework or library protections: No framework or library protection is visible. PHP's `header()` directly sends the supplied HTTP header and does not enforce origin validation. No route-level or server-level CORS restriction is shown in the available context.
7. Step 6 / privilege or authentication needed: The exact authentication requirement is not visible. However, this is top-level PHP code; whenever this script is reachable and executed, it unconditionally emits the permissive CORS header on line 6.
8. Step 7 / concrete impact: The response appears to expose generated OpenAPI YAML from `./src` via `$openapi->toYaml()` on lines 4 and 12. With line 6, any website origin can read that response if the endpoint is reachable, potentially exposing API structure, routes, parameters, and security scheme metadata.
9. Step 8 / weakest link: The weakest link is the unconditional wildcard CORS policy on line 6. There is no visible allowlist or origin validation restricting which sites may read the generated OpenAPI response.

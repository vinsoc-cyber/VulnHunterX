# yaml.github-actions.security.run-shell-injection.run-shell-injection @ .github/workflows/docker-image.yml:29

**Verdict:** TP · **Confidence:** Medium (0.76) · **Truth:** not-real · **Grade:** FALSE-ALARM · **Iterations:** 5

## Reasoning

The flagged `run:` block directly interpolates `github.ref` into shell commands on lines 33-34 with no visible shell-escaping defense; double quotes do not prevent command substitution after GitHub expression expansion. Although exact repository permissions are unavailable, the vulnerable GitHub Actions shell-injection pattern is present and could be exploitable by a user able to create/select a malicious ref and trigger the workflow.

## Data flow

Potentially attacker-controlled GitHub Actions ref value `github.ref` (lines 33-34) → direct GitHub expression interpolation into shell script under `run: |` (line 29) → shell parses `VERSION=$(echo "${{ github.ref }}" | sed -e 's,.*/\(.*\),\1,')` on line 33 and `[[ "${{ github.ref }}" == "refs/tags/"* ]] ...` on line 34 → optional transformations to `VERSION` on lines 34-35 → Docker tag argument on line 42. Additional, less concerning flows: `github.repository_owner` on line 31 → `IMAGE_ID` → `tr` lowercasing on line 32 → Docker tags on lines 42-43; `github.sha` on line 36 → `cut -c 1-7` into `COMMIT` → Docker tag on line 43.

## Answers

1. Flagged line located: line 29 is exactly `run: |`. It is in `.github/workflows/docker-image.yml`, job `build`, step `Build and push multi-arch DVWA image` beginning at lines 28-29. The rule-described construct is present because this shell `run:` block directly interpolates GitHub context expressions on lines 31, 33, 34, and 36.
2. Step 1 source: the potentially dangerous data originates from GitHub Actions `github` context data: `github.repository_owner` on line 31, `github.ref` on lines 33-34, and `github.sha` on line 36. The most relevant potentially attacker-influenced source is `github.ref`, because branch or tag refs can contain user-chosen names in some workflow scenarios.
3. Step 2 trace: `github.repository_owner` flows into `IMAGE_ID` on line 31, then through lowercasing with `tr` on line 32, then into Docker tag arguments on lines 42-43. `github.ref` flows into `VERSION` through `VERSION=$(echo "${{ github.ref }}" | sed -e 's,.*/\(.*\),\1,')` on line 33, is interpolated again in the shell conditional on line 34, may be transformed by `sed` on line 34 or set to `latest` on line 35, then flows into the Docker tag on line 42. `github.sha` flows into `COMMIT` through `cut -c 1-7` on line 36, then into the Docker tag on line 43.
4. Step 3 validation/sanitization: line 32 lowercases `IMAGE_ID`, lines 33-34 perform `sed` extraction/comparison for `VERSION`, and line 36 truncates the SHA. These are formatting transformations, not shell-injection sanitization. In particular, the `${{ github.ref }}` expression on lines 33-34 is expanded into the shell script before execution; placing it inside shell double quotes on line 33 does not prevent command substitution if the expanded ref contains shell metacharacters such as `$()`.
5. Step 4 sink: the sink is shell execution in the GitHub Actions `run:` block beginning at line 29. The dangerous operation is direct expression interpolation of GitHub context data into shell syntax on lines 31, 33, 34, and 36 before the shell interprets the script.
6. Step 5 framework/library protections: no automatic protection is visible in the workflow. GitHub Actions expression interpolation is not shell escaping. The workflow does not use the recommended protection of assigning the GitHub context value to an `env:` variable and referencing it as a quoted shell variable such as `"$REF"` inside the script.
7. Step 6 privilege/authentication: the visible triggers are `workflow_dispatch` on line 4 and `push` to branch `master` on lines 5-7. For the `push` trigger, an attacker would need the ability to push to `master`, and the ref would be constrained to `refs/heads/master` for that trigger. For `workflow_dispatch`, repository permissions and selectable refs are not shown; an attacker would likely need at least repository permissions sufficient to create/select a malicious ref and run the workflow. Exact branch protections and Actions permissions were requested but are unavailable.
8. Step 7 impact: if an attacker can control a ref value reaching `github.ref` on lines 33-34, the impact is shell command injection/RCE on the GitHub Actions runner. This could expose checked-out source code and workflow credentials, including the `GITHUB_TOKEN` used by the registry login step on lines 21-26, and could affect the image pushed by the Docker build on lines 41-44.
9. Step 8 weakest link: the weakest link is direct `${{ github.ref }}` interpolation into the shell `run:` block on lines 33-34 without shell-safe environment-variable indirection. The visible workflow contains no adequate defense against shell interpretation of attacker-controlled ref content.

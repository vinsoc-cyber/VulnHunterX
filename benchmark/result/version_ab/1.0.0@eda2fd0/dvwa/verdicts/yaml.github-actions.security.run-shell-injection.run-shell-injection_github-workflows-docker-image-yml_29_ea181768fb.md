# yaml.github-actions.security.run-shell-injection.run-shell-injection @ .github/workflows/docker-image.yml:29

**Verdict:** TP · **Confidence:** Medium (0.78) · **Truth:** not-real · **Grade:** FALSE-ALARM · **Iterations:** 5

## Reasoning

The reported construct is present: GitHub context data, especially `${{ github.ref }}`, is directly interpolated into a shell `run:` block with no visible shell-injection-safe handling. The additional requested repository/permission context is unavailable and shows no defense; based on the visible workflow, an exploitable CWE-78 pattern exists if an attacker can influence the ref used to trigger the workflow.

## Data flow

GitHub Actions context source `${{ github.ref }}` (`.github/workflows/docker-image.yml`:33-34), reached through `workflow_dispatch` (`.github/workflows/docker-image.yml`:4) or `push` to `master` (`.github/workflows/docker-image.yml`:5-7) → direct interpolation into flagged shell `run:` block (`.github/workflows/docker-image.yml`:29) → shell assignment and command execution context in `VERSION=$(echo "${{ github.ref }}" | sed ...)` (`.github/workflows/docker-image.yml`:33) and shell conditional (`.github/workflows/docker-image.yml`:34) → derived `VERSION` echoed (`.github/workflows/docker-image.yml`:38) and used in `docker buildx build --tag $IMAGE_ID:$VERSION --push` (`.github/workflows/docker-image.yml`:41-44)

## Answers

1. Step 1: The dangerous data originates from GitHub Actions context values inserted into a shell `run:` block. The primary source is `${{ github.ref }}` on lines 33 and 34, which represents the workflow event ref and can be influenced through branch/tag/ref names in GitHub events. Other context values are `${{ github.repository_owner }}` on line 31 and `${{ github.sha }}` on line 36.
2. Step 2: Data flow: `${{ github.repository_owner }}` is interpolated into `IMAGE_ID=ghcr.io/${{ github.repository_owner }}/dvwa` on line 31, then transformed by `tr '[A-Z]' '[a-z]'` on line 32, then used in Docker tags on lines 42 and 43. `${{ github.ref }}` is interpolated into `VERSION=$(echo "${{ github.ref }}" | sed -e 's,.*/\(.*\),\1,')` on line 33, interpolated again in the shell conditional on line 34, possibly transformed by `sed -e 's/^v//'` on line 34, compared/overwritten on line 35, echoed on line 38, and used in a Docker tag on line 42. `${{ github.sha }}` is interpolated into `COMMIT=$(echo "${{ github.sha }}" | cut -c 1-7)` on line 36, echoed on line 39, and used in a Docker tag on line 43.
3. Step 3: There is no visible validation, shell escaping, allowlist, or safe `env:` indirection before interpolation into the shell script. The `tr`, `sed`, and `cut` operations on lines 32, 33, 34, and 36 are transformations after interpolation into shell code, not sufficient sanitization for CWE-78. Double quotes around `${{ github.ref }}` on lines 33 and 34 are not a complete defense because GitHub expression interpolation occurs before shell execution.
4. Step 4: The sink is the shell `run:` block beginning at the exact flagged line, line 29: `run: |`. The dangerous operation is executing shell code containing directly interpolated GitHub context values on lines 31, 33, 34, and 36.
5. Step 5: No automatic framework or library protection is visible. GitHub Actions does not automatically shell-escape `${{ ... }}` expressions placed directly into a `run:` script. The safer pattern recommended by the rule — assigning context values to `env:` and referencing quoted shell variables such as `"$REF"` — is not used.
6. Step 6: The workflow can be triggered by `workflow_dispatch` on line 4 or by `push` to `master` on lines 5-7. Exact repository permissions, branch protection, and workflow-dispatch policy were requested but are unavailable, so the exact attacker privilege level is not visible. At minimum, an attacker would need the ability to cause this workflow to run with a crafted/ref-influenced event, such as sufficient repository permissions to push/create refs or dispatch the workflow.
7. Step 7: If an attacker controls the interpolated ref/context data, the security impact is shell command injection/RCE on the GitHub Actions runner. This can lead to theft or misuse of CI credentials, including the `GITHUB_TOKEN` used in the GHCR login step on lines 22-26, and manipulation of the image build/push operation on lines 41-44.
8. Step 8: The weakest link is the direct use of `${{ github.ref }}` inside shell code on lines 33 and 34 without visible sanitization or `env:` indirection. No complete defense is visible in the provided code or the additional unavailable context.

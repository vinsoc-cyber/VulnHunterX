# php.lang.security.injection.tainted-filename.tainted-filename @ vulnerabilities/view_help.php:20

**Verdict:** FP · **Confidence:** High (0.86) · **Truth:** real · **Grade:** MISS · **Iterations:** 3

## Reasoning

The new context is unavailable and does not change the visible data flow: tainted GET parameters reach `file_get_contents`, but only after a fixed local relative prefix `../vulnerabilities/` defined on line 3 and used on lines 21 and 23. Because the attacker-controlled data cannot appear at the beginning of the filename as a URL scheme, the specific reported CWE-918 SSRF issue is not clearly exploitable, even though separate local file inclusion or eval-related risks may remain.

## Data flow

`$_GET['id']` and `$_GET['locale']` user input checked only for existence in `vulnerabilities/view_help.php` lines 11-13 → assigned to `$id` and `$locale` on lines 15 and 17 → `$locale` compared to `'en'` on flagged line 20 → `$id` interpolated into `../vulnerabilities/{$id}/help/help.php` at line 21, or `$id` and `$locale` interpolated into `../vulnerabilities/{$id}/help/help.{$locale}.php` at line 23 → resulting local-prefixed filename passed to `file_get_contents(...)` and then `eval(...)` on lines 21 and 23

## Answers

1. Step 1: The potentially dangerous data originates from HTTP GET user input: `$_GET['id']`, `$_GET['security']`, and `$_GET['locale']`. The code only checks that these keys exist on lines 11-13, then assigns them to local variables on lines 15-17.
2. Step 2: Data flow: `$_GET['id']` is assigned to `$id` on line 15 and interpolated into the filename passed to `file_get_contents(...)` on lines 21 and 23. `$_GET['locale']` is assigned to `$locale` on line 17, compared against the literal `'en'` on line 20, and if not equal, interpolated into the filename on line 23. `$_GET['security']` is assigned to `$security` on line 16 but is not used in the visible sink path. The exact flagged line is `if ($locale == 'en') {` at line 20, in top-level script scope rather than a named function.
3. Step 3: There is no validation, sanitization, canonicalization, or allowlist for `$id` or non-`en` `$locale` in the provided code. The only visible controls are parameter-existence checks on lines 11-13 and the `$locale == 'en'` branch on line 20. However, for the specific SSRF class, the filename passed to `file_get_contents` is constructed with a fixed local relative prefix: `DVWA_WEB_PAGE_TO_ROOT . "vulnerabilities/` on lines 21 and 23, and `DVWA_WEB_PAGE_TO_ROOT` is visibly defined as `'../'` on line 3.
4. Step 4: The sink is `file_get_contents(...)` on lines 21 and 23. The dangerous operation for this Semgrep rule would be using attacker-controlled input as a filename/URL, allowing PHP URL wrappers such as `http://` to trigger server-side requests. The returned content is also passed to `eval(...)` on lines 21 and 23, but that is a separate issue from the reported CWE-918 SSRF rule.
5. Step 5: No framework or library automatic filename protection is visible. `dvwaPageStartup(array('authenticated'))` is called on line 6, but its implementation remains unavailable in the added context. PHP does not automatically sanitize path components. For SSRF specifically, PHP stream-wrapper URLs require the URL scheme to appear at the beginning of the filename string; here the visible filename begins with the fixed local prefix `../vulnerabilities/` before any user-controlled `$id` or `$locale` on lines 21 and 23.
6. Step 6: The visible code calls `dvwaPageStartup(array('authenticated'))` on line 6, which indicates the intended access state is authenticated. The implementation of `dvwaPageStartup` was unavailable in the additional context, so the exact enforcement details are not visible.
7. Step 7: If attacker input could control a full URL passed to `file_get_contents`, the impact would be SSRF, potentially allowing the server to make requests to internal or external network services. In this specific code, the visible fixed local prefix prevents the attacker-controlled portions from becoming the leading URL scheme for `file_get_contents`, so the concrete CWE-918 SSRF impact is not demonstrated. Other impacts, such as local file inclusion or code execution through `eval`, may exist but are outside the specific reported rule.
8. Step 8: The weakest visible defense is the lack of allowlist or canonicalization for `$id` and `$locale` before building a file path on lines 21 and 23. For the specific SSRF finding, however, the defense is that the sink argument starts with the fixed local prefix defined on line 3 and used on lines 21 and 23, so user input cannot make the `file_get_contents` argument begin with a remote URL scheme.

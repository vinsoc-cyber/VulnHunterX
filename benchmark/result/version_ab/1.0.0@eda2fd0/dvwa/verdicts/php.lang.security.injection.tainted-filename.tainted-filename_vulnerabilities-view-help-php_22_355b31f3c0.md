# php.lang.security.injection.tainted-filename.tainted-filename @ vulnerabilities/view_help.php:22

**Verdict:** FP · **Confidence:** High (0.86) · **Truth:** real · **Grade:** MISS · **Iterations:** 3

## Reasoning

The reported class is SSRF via tainted filename, but the attacker-controlled `$id` and `$locale` are embedded only after a fixed local relative prefix, `../vulnerabilities/`, so they cannot form the leading URL/stream-wrapper scheme required for SSRF. The code may still be dangerous for other vulnerability classes, especially path traversal or unsafe eval, but the specific CWE-918 finding is not supported by the visible data flow.

## Data flow

source `$_GET['id']` (line 13) and `$_GET['locale']` (line 15) → no sanitization; `$locale == 'en'` branch check only (line 18) → interpolation after fixed local prefix `DVWA_WEB_PAGE_TO_ROOT . 'vulnerabilities/'`, where `DVWA_WEB_PAGE_TO_ROOT` is defined as `'../'` (lines 3, 19, 22) → `file_get_contents()` filename sink (lines 19, 22) → returned content passed to `eval()` (lines 19, 22)

## Answers

1. Step 1: The new context is unavailable and does not change the source analysis. The dangerous data originates from HTTP GET user input: `$_GET['id']`, `$_GET['security']`, and `$_GET['locale']` are checked for existence on lines 10-12, then assigned on lines 13-15. The filename-relevant inputs are `$id` from `$_GET['id']` on line 13 and `$locale` from `$_GET['locale']` on line 15.
2. Step 2: The exact flagged line is line 22: `eval( '?>' . file_get_contents( DVWA_WEB_PAGE_TO_ROOT . "vulnerabilities/{$id}/help/help.{$locale}.php" ) . '<?php ' );`. It lives in top-level PHP script scope, shown as Function: `<unknown>`, not in a named function. Data flow: `$_GET['id']` → `$id` on line 13 → interpolated into the filename on lines 19 and 22. `$_GET['locale']` → `$locale` on line 15 → compared to `'en'` on line 18 → if not `'en'`, interpolated into the filename on line 22.
3. Step 3: The new requested context for `dvwaPageStartup`, `dvwaPageNewGrab`, `DVWA_WEB_PAGE_TO_ROOT`, `allow_url_fopen`, and `allow_url_include` was unavailable, so it adds no visible validation or sanitization. In the provided code, there is no sanitization of `$id` or `$locale`. The checks on lines 10-12 only verify presence. The comparison `$locale == 'en'` on line 18 is not a general validation for `$locale`. However, for the specific SSRF/tainted URL filename risk, the file path is visibly prefixed with `DVWA_WEB_PAGE_TO_ROOT . "vulnerabilities/"`, and `DVWA_WEB_PAGE_TO_ROOT` is defined as the relative local prefix `'../'` on line 3.
4. Step 4: The sink is `file_get_contents()` on lines 19 and 22. The dangerous operation for this rule would be using attacker-controlled input as a filename/URL that PHP could resolve through a remote stream wrapper, causing SSRF. The returned file contents are also passed into `eval()` on lines 19 and 22, but that is a separate code-execution/LFI concern, not the reported CWE-918 SSRF issue.
5. Step 5: No framework or library automatic protections are visible. `dvwaPageStartup(array('authenticated'))` is called on line 6, but its implementation was unavailable, so no sanitization or stream-wrapper restriction can be credited from it. The visible protection relevant to SSRF is the fixed local relative prefix: `DVWA_WEB_PAGE_TO_ROOT` is defined as `'../'` on line 3, and both sink paths begin with `../vulnerabilities/` on lines 19 and 22 before any user-controlled `$id` or `$locale` appears.
6. Step 6: The visible code requires an authenticated user because `dvwaPageStartup(array('authenticated'))` is called on line 6. The implementation is unavailable, so the exact authentication mechanism and whether any stronger role such as admin is required is not visible in provided context.
7. Step 7: If the attacker controlled a full URL passed directly to `file_get_contents()`, the concrete impact for this rule would be SSRF: server-side requests to attacker-chosen internal or external URLs. In this code, the attacker-controlled values are not at the start of the filename; they appear after the fixed relative prefix on lines 19 and 22, so remote URL-wrapper SSRF is not clearly achievable. Other impacts such as path traversal, local file inclusion, or unsafe `eval()` may exist, but they are outside the specific reported SSRF rule.
8. Step 8: The weakest visible link overall is lack of allowlist validation for `$id` and `$locale` before constructing a filename on lines 19 and 22. But for the specific CWE-918 SSRF finding, the visible defense is complete enough: the filename passed to `file_get_contents()` starts with a fixed local relative path, `../vulnerabilities/`, from line 3 and lines 19/22, so user input cannot supply the leading `http://`, `https://`, or other stream-wrapper scheme needed to turn this into SSRF.

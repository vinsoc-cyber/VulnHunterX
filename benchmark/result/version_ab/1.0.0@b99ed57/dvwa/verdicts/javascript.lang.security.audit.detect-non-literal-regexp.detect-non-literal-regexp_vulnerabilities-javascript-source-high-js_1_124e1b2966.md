# javascript.lang.security.audit.detect-non-literal-regexp.detect-non-literal-regexp @ vulnerabilities/javascript/source/high.js:1

**Verdict:** FP · **Confidence:** High (0.9) · **Truth:** not-real · **Grade:** CORRECT · **Iterations:** 3

## Reasoning

The additional requested context is unavailable and does not change the visible analysis: the flagged non-literal RegExp sink is present, but its dynamic fragment is generated from a constant numeric loop counter and constrained to alphanumeric characters. Because no attacker-controlled source reaches the RegExp pattern and the resulting regex is a simple word-boundary token match, the CWE-1333/ReDoS finding is not exploitable on the shown path.

## Data flow

vulnerabilities/javascript/source/high.js:1 `var a=[...]` local array literal → line 1 local rotation `(function(c,d){...}(a,0x1f4))` → line 1 decoder `b=function(c,d){...}` → line 1 eval/unpacker call with constants `e=0x3e`, `f=0x137`, `i={}` → line 1 local encoder `h=function(j){...}` transforms numeric loop counter into alphanumeric token → line 1 `while(f--)` calls `h(f)` → line 1 concatenation `'\x5cb'+h(f)+'\x5cb'` → line 1 sink `new RegExp(...,'g')` → line 1 replacement `d[b('0x2')](...,g[f])`

## Answers

1. Step 0 / location: The flagged line is present at vulnerabilities/javascript/source/high.js:1. The exact relevant flagged text is `d=d[b('0x2')](new RegExp('\x5cb'+h(f)+'\x5cb','g'),g[f]);`. This is inside an anonymous unpacker function passed to `eval(function(d,e,f,g,h,i){...})` on line 1, at top-level script scope. The rule-described construct is present: `new RegExp(...)` is called with a non-literal expression containing `h(f)`.
2. Q1: The new context does not reveal any external source. In the visible code, the RegExp pattern data originates from local constant arguments to the anonymous eval/IIFE call on line 1: `e=0x3e`, `f=0x137`, and local function `h=function(j){...}`. No user input, file, network, database, request parameter, DOM input, or other attacker-controlled source is visible. The requested `global:a`, `global:b`, and caller context are unavailable and do not add evidence of attacker control.
3. Q2: Full visible data trace on line 1: `var a=[...]` initializes a local/top-level constant array literal → `(function(c,d){...}(a,0x1f4))` rotates that local array → `var b=function(c,d){...return e;};` reads entries from `a` → `eval(function(d,e,f,g,h,i){...}(b('0x7'),0x3e,0x137,b('0x8')[b('0x9')]('|'),0x0,{}))` invokes the unpacker with constant numeric `e` and `f` → inside the unpacker, `h=function(j){...}` converts numeric values to token strings → `while(f--)` passes the numeric loop counter into `h(f)` → `new RegExp('\x5cb'+h(f)+'\x5cb','g')` builds the regex → that regex is used in `d[b('0x2')](...,g[f])`, i.e. a decoded `replace` operation.
4. Q3: No explicit regex validation library is used. However, there is a specific visible defense in the data construction on line 1: `h(f)` is derived from the numeric loop counter `f`, initialized as constant `0x137`, and encoded by modulo/base conversion. For values `0..35`, `j.toString(0x24)` yields digits/lowercase letters; for values above `0x23`, `String.fromCharCode(j+0x1d)` with `j % 0x3e` yields uppercase letters. Thus the dynamic regex fragment is constrained to alphanumeric characters, not regex metacharacters. The final pattern is also only word-boundary-delimited: `'\x5cb'+h(f)+'\x5cb'`.
5. Q4: The sink is `new RegExp('\x5cb'+h(f)+'\x5cb','g')` at vulnerabilities/javascript/source/high.js:1. Dynamic regex compilation can be dangerous when attacker-controlled input creates catastrophic-backtracking patterns. Here, the operation is not unsafe in the visible code because the dynamic portion is locally generated alphanumeric token text, and the regex shape is a simple `\bTOKEN\b` global match.
6. Q5: No framework or library automatic protection is visible. This is raw obfuscated JavaScript, not an ORM/template/framework context. The relevant protection is not framework-provided; it is the visible local data constraint from constant numeric loop counter to alphanumeric token before `new RegExp` on line 1.
7. Q6: Authentication or privilege state is not visible. The code appears to execute at script load time on line 1. However, for this flagged sink, no attacker-controllable input path is visible regardless of authentication state.
8. Q7: If an attacker could control the regex pattern, the theoretical impact would be Regular Expression Denial of Service by forcing expensive regex evaluation. In this provided code, that impact is not demonstrated because the pattern is constrained to simple alphanumeric word tokens and the searched string `d` is also supplied from local decoded constants on line 1.
9. Q8: No exploitable weak link is visible for this flagged RegExp sink. The defense is complete for the shown path because the only dynamic regex fragment, `h(f)`, is generated from a constant numeric loop counter and encoded to alphanumeric characters before reaching `new RegExp`; there is no visible attacker-controlled source and no regex metacharacter injection path.

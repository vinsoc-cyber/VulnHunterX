# javascript.lang.security.audit.detect-non-literal-regexp.detect-non-literal-regexp @ vulnerabilities/javascript/source/high.js:1

**Verdict:** FP · **Confidence:** High (0.88) · **Truth:** not-real · **Grade:** CORRECT · **Iterations:** 3

## Reasoning

The second-round context did not reveal any external source influencing the flagged regex; unavailable context adds no evidence of taint, while the visible line shows the regex argument is derived from constants and a bounded numeric loop counter. The flagged dynamic `RegExp` pattern is therefore non-literal but not attacker-controlled or visibly ReDoS-prone on the shown path.

## Data flow

vulnerabilities/javascript/source/high.js:1 static array literal `a=[...]` → line 1 constant array rotation `(function(c,d){...}(a,0x1f4))` → line 1 lookup helper `b(...)` over local static `a` → line 1 anonymous unpacker call `eval(function(d,e,f,g,h,i){...}(b('0x7'),0x3e,0x137,b('0x8')[b('0x9')]('|'),0x0,{}))` → line 1 local formatter `h=function(j){...}` → line 1 bounded loop `while(f--)` where `f` starts as constant `0x137` → line 1 pattern `'\x5cb'+h(f)+'\x5cb'` → line 1 sink `new RegExp(...,'g')` → line 1 use in `replace` expression `d[b('0x2')](...,g[f])`

## Answers

1. Step 0 / flagged line: The flagged line is present at `vulnerabilities/javascript/source/high.js:1`. The rule-described construct is present in the exact expression `new RegExp('\x5cb'+h(f)+'\x5cb','g')`, within `d=d[b('0x2')](new RegExp('\x5cb'+h(f)+'\x5cb','g'),g[f]);`. It lives inside the anonymous unpacker function `function(d,e,f,g,h,i){...}` that is passed directly to `eval(...)` on the same top-level minified line, not inside a named function.
2. Q1: The potentially dangerous data would be the first argument to `RegExp`. In the visible code, it originates from constants and a local numeric loop counter: the anonymous unpacker is invoked with constant arguments including `0x3e`, `0x137`, `0x0`, and `{}` on line 1. The additional context for `global:a`, `function:b`, and the anonymous unpacker was unavailable and does not add any external/user-controlled source.
3. Q2: Data flow with additional context included: `a` is initialized as a static array literal on line 1; the IIFE `(function(c,d){...}(a,0x1f4))` rotates that local array using constant `0x1f4` on line 1; `b=function(c,d){c=c-0x0;var e=a[c];return e;}` reads from that static array on line 1; the unpacker is invoked as `function(d,e,f,g,h,i)(b('0x7'),0x3e,0x137,b('0x8')[b('0x9')]('|'),0x0,{})` on line 1; inside it, `h=function(j){...}` deterministically formats numeric `j`; `while(f--)` iterates from constant `0x137`; the regex pattern is built as `'\x5cb'+h(f)+'\x5cb'`; then it reaches `new RegExp(...,'g')` on line 1.
4. Q3: No explicit validation, sanitization, escaping, or regex-safety library is applied before `new RegExp` on line 1. However, the visible code provides a concrete source/type constraint: `f` is supplied as the numeric constant `0x137`, decremented in `while(f--)`, and passed to deterministic formatter `h(f)`. The resulting pattern is a word-boundary-wrapped generated token, not attacker-provided regex syntax.
5. Q4: The sink is `new RegExp('\x5cb'+h(f)+'\x5cb','g')` at line 1, immediately used as the search pattern in `d[b('0x2')](...,g[f])`, where `b('0x2')` resolves to the string method name `replace` from the local lookup table. The dangerous operation would be dynamic regex construction and execution, but exploitability for CWE-1333 requires an attacker-controlled or catastrophically complex pattern, which is not visible here.
6. Q5: No framework or library automatic protection is visible or relevant. This is raw JavaScript-style obfuscated code in `vulnerabilities/javascript/source/high.js`; there is no ORM, template engine, request framework, or regex-safety middleware shown.
7. Q6: The required privilege/authentication state is not visible in the provided context. The code appears top-level and likely executes when the script is loaded, but no route, request handler, or authentication boundary is shown. The additional caller context remains unavailable.
8. Q7: If an attacker controlled the regex pattern, the security impact would be Regular Expression Denial of Service by blocking the JavaScript main thread/event loop. In the visible flagged path, the pattern is generated from constants and a bounded numeric loop counter, so that impact is not demonstrated.
9. Q8: The weakest link considered was provenance of the dynamic regex pattern inside eval-obfuscated code. After the second round, no new context shows an external source; the visible source constraint remains the complete defense for this finding: constant unpacker arguments plus deterministic numeric formatting prevent attacker control of the regex pattern on the flagged path.
